# 콕끼리 배드민턴 홈페이지 — 프로젝트 인수인계 (다른 AI/작업자용)

> 이 문서 하나로 프로젝트 전체를 이해하고 바로 이어서 작업할 수 있게 정리한 기술 인수인계서다.
> - 요청(사용자 지시) 이력과 상태: [`메인.md`](메인.md) — 단일 기준 문서, 새 요청은 여기에 번호로 추가.
> - 요청별 상세 작업 기록: [`기록/`](기록/) 폴더(YYYY-MM-DD_HH-mm-ss_핵심내용.md).
> - 작업 방식 규칙: [`CLAUDE.md`](CLAUDE.md)(Claude 세션), [`AGENTS.md`](AGENTS.md)(Codex 세션).
> - 이 인수인계서는 "시스템이 어떻게 돌아가는가"를 설명한다(요청 로그는 메인.md 참고).
> - 최초 작성: 2026-09-13 KST. 이후 큰 구조 변경이 있으면 이 문서도 갱신할 것.

---

## 0. 한 줄 요약

배드민턴 모임 "콕끼리"의 회원용 홈페이지. **공지사항 · 대진표(경기 편성/승패 기록/점수 정산) · 시드현황(회원·게스트 랭킹)** 을 제공한다. Cloudflare Workers + D1(SQLite)로 돌아가고, GitHub `main`에 push하면 자동 배포된다.

- **공개 주소**: https://kokkiri.kokkiri-badminton.workers.dev
- **저장소**: GitHub `rkdtjdwns1312-wq/-` (origin), 폴더 `kokkiri-site/`.

---

## 1. 작업 환경과 배포 (먼저 읽기)

- **빌드 도구 없음**: `dist/server/*.js` 파일이 곧 배포물이다(번들러/트랜스파일 없음). 파일을 고치고 push하면 그대로 돈다.
- **git·node가 PATH에 없다.** Codex 번들을 쓴다:
  - git: `C:\Users\just\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\git\cmd\git.exe`
  - node(v24): `C:\Users\just\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe`
- **작업 절차**: `git pull --ff-only origin main` → 코드 수정 → `node check-login.mjs`(PASS 확인) → `git commit` → `git push origin main` → Cloudflare Workers Builds가 `wrangler d1 migrations apply DB --remote && wrangler deploy` 자동 실행.
- **배포 확인**: 공개 주소나 Cloudflare 대시보드(Workers & Pages → kokkiri → Deployments). 확인 전에는 "배포 완료"라고 적지 않는다.
- **테스트**: `check-login.mjs`(node:sqlite 인메모리 DB로 서버를 그대로 구동). 새 마이그레이션을 추가하면 `check-login.mjs` 상단의 마이그레이션 목록에도 넣는다. `node check-login.mjs --serve`로 http://localhost:4173 로컬 미리보기(운영진: `/operate-test-editor-key`, 비번 `test-password`).
- **협업 주의(중요)**: Claude와 Codex가 **같은 워킹트리**를 공유한다. 동시에 작업하면 커밋이 겹치거나 서로의 미커밋 변경을 함께 커밋할 수 있다. **한 번에 한 도구로**, 작업 전 pull, 작업 후 곧바로 commit·push. 강제 push·hard reset 금지, 로컬 변경 보존.
- **PowerShell 커밋 팁**: 커밋 메시지에 `→ … ? "` 같은 문자를 here-string에 넣으면 파싱이 깨진다. ASCII 위주의 간단한 메시지를 쓴다.

## 1-1. 절대 건드리면 안 되는 것 / 제약

- `wrangler.jsonc`의 **`"keep_names": false`** — 지우면 회원 화면이 빈 화면이 된다(esbuild `__name()` 헬퍼가 인라인 클라이언트를 깨뜨림). 배포 후 `__name`이 페이지 소스에 0개여야 정상.
- **클라이언트는 `client(EDITOR)` 함수 하나를 통째로 문자열로 인라인**한다(`page()`에서 `client.toString()`). 따라서 이 함수 안에서는 **바깥 스코프(모듈 변수/함수)를 참조하면 안 된다** — 런타임 ReferenceError. 필요한 건 함수 안에 넣거나 인자로 넘긴다. (테스트의 `new Function('return ('+client.toString()+')')`는 문법만 검사하고 참조 오류는 못 잡으니 주의.)
- **비밀값 금지**: 운영진 비밀번호, 운영진 비밀 주소(`/operate-<EDITOR_KEY>`), `EDITOR_KEY`·`OPERATOR_PASSWORD` 값, 토큰을 md·소스·주석에 남기지 않는다. 이 값들은 Cloudflare 대시보드 Secrets에 사용자가 직접 넣는다.
- 이미지는 `public/` 정적 자산으로 제공(무료 플랜 번들 3MB 제한 때문에 서버 코드에 큰 파일 내장 금지). `dist/server/images.js`는 빈 스텁.
- 줄바꿈은 `.gitattributes`로 LF 통일.
- 프로젝트 루트(`배드민턴 프로그램 만들기/`)의 `index.html`·`app.js`·`firebase.json` 등은 2026-09-10 첫 Firebase 시도 잔재로 현재 홈페이지와 무관. 삭제하지 말고 방치.

---

## 2. 파일 구조 (`dist/server/`)

| 파일 | 역할 |
| --- | --- |
| `index.js` | Worker 진입점(`export default { fetch }`) — `boards.js`로 위임. |
| `boards.js` | **서버 핵심**: 라우팅, HTML 페이지 렌더(`page()`), 스케줄 검증(`validate`), 정산(`settleSchedule`), 마감 취소(`unsettleSchedule`), API 엔드포인트. |
| `boards-client.js` | **회원/운영진 화면(SPA)** — `client(EDITOR)` 함수 하나. 해시 라우팅(home/notice/schedule/seed/post), 대진 편성 picker, 승패 기록, 시드 표 등 모든 화면 로직. `client.toString()`으로 페이지에 인라인됨. |
| `boards-style.js` | 전체 CSS(`export const css`). 일부 표 스타일은 boards-client.js가 런타임에 `document.head`에 주입하는 `extraStyle`에도 있다(순위 화살표 색 등은 여기서 이김 — 겹치는 규칙 수정 시 주의). |
| `roster.js` | **명단 원본**: 회원 61명 + 게스트 32명 배열 `[{id,name,seed,type,points?}]`. 게스트에는 points 포함. |
| `rankings.js` | 시드 계산의 기준: `initialRankingRows`(회원 이름·점수·원본순위), `seedForPoints`(점수→급수 구간), 회원 시드행 생성 함수들, `orderRankingRows`(점수순 재정렬·순위/시드 재계산). |
| `operator-login.js` | 운영진 로그인 처리(`/api/operator-login`), 시도 횟수 제한. |
| `images.js` | 빈 스텁(이미지는 public/). |
| `roster-ui.js` | (레거시로 추정) 옛 명단 UI. 현재 메인 흐름은 boards-client.js. 새로 참조하지 말 것. |

- 마이그레이션: `drizzle/0000~0005*.sql`. `check-login.mjs`에 목록이 있으니 새 파일 추가 시 함께 갱신.
- 설정: `wrangler.jsonc`(Worker 이름 kokkiri, D1 바인딩 DB, migrations_dir drizzle, assets public, keep_names:false, workers_dev:true).

---

## 3. 데이터 모델 (D1 / SQLite)

- **`schedules`**: 옛 단일 대진표(id=1). `preserveLegacy()`가 이걸 `board_posts`의 `legacy-schedule`로 1회 복사.
- **`board_posts`**: 공지+대진표 게시글. `(id, kind['notice'|'schedule'], payload(JSON), version, last_operation, created_at, updated_at)`. 낙관적 동시성은 `version`으로.
- **`ranking_members`**: 회원 랭킹 상태 `(member_id, name, points, seed, rank, previous_rank, attendance, wins, losses, updated_at)`. `ensureRankingMembers()`가 `rankings.js`+`roster.js`로 최초 시드(INSERT OR IGNORE).
- **`ranking_settlements`**: 정산 1건당 1행 `(schedule_id, settled_at, operation)`.
- **`ranking_events`**: 정산 시 회원별 델타 기록 `(schedule_id, member_id, attendance_points, win_points, loss_points, total_points, points_before/after, rank_before/after, seed_before/after, created_at)`. **마감 취소가 이 델타를 역산**해 되돌린다.
- **`operator_login_limits`**: 로그인 시도 제한.

### 대진표 payload(JSON) 형태
```
{
  title, names[], participantIds[],  // names[i] ↔ participantIds[i] 같은 순서(정렬 매칭에 중요)
  courts, rounds,
  schedule: [ { round, method['same'|'balanced'|'random'], g:[[a1,a2,b1,b2],...], rest:[...] }, ... ],
  results: { "<roundIndex>-<courtIndex>": "a"|"b", ... },
  settledAt: null | ISO,   // 정산(마감) 시각
  mvp: [name,...],         // 정산 시 채워짐(회원만, 아래 MVP 규칙)
  preTitle: 원제목          // 정산 시 저장 → 마감 취소가 제목 복원에 사용
}
```
- 서버 `validate()`가 payload를 화이트리스트로 정리한다. `roundMethods`·`late` 같은 **생성 전용 UI 상태는 저장 안 됨**(클라이언트에서 생성 시에만 사용). 단 `method`(라운드 방식)와 `mvp`는 저장한다.

---

## 4. 인증/권한 모델 (중요한 설계 전제)

- **회원 로그인이 없다.** 인증은 운영진 하나뿐: 비밀 주소 `/operate-<EDITOR_KEY>` 로 들어오거나, 회원 화면 오른쪽 아래 **'운영진권한'** 버튼+`OPERATOR_PASSWORD`. 로그인 성공 시 `EDITOR`(=키)가 클라이언트에 주입되고, 저장·정산 등에 `x-kokkiri-editor` 헤더로 쓰인다.
- 따라서 **"회원"은 사실상 "링크를 가진 누구나"** 이다. 승패 기록(`/result`)은 이 전제로 **누구나 정산 전까지 기록 가능**하게 열려 있다(요청 037). 최종 확정(마감/정산)과 삭제·마감취소는 운영진 전용.
- 점수 규칙: **정모 출석 +1(모임당 1회), 승 +1, 패 -1**. 점수 누계로 급수(시드) 자동 계산(`seedForPoints` 구간). **랜덤 라운드는 승패를 점수에 반영하지 않는다.**

---

## 5. 주요 화면과 기능 (동작 방식)

### 대진 편성(운영진, `openPicker`)
- 코트 수·라운드 수, **라운드별 매칭 방식** 선택: `동일`(점수 가까운 사람끼리) / `인접`(실력 균형=강약 섞기) / `랜덤`(점수 무관). 시드 점수를 반영해 `generate()`가 편성.
- 참가자 체크(회원/게스트 탭). **급히 온 게스트는 "추가하기"로 즉석 추가**(임시, 이 대진에만; 랭킹/시드표 미반영, id는 임시 uuid).
- **늦참** 토글. 쉬는 순서 우선도: 게스트 > 늦참 > 운영진 > 회원(회원이 가장 안 쉼, 같은 등급끼리는 공평 로테이션).
- 저장 시 `names[]`와 `participantIds[]`를 같은 순서로 만든다(정산에서 이름↔회원 매핑에 사용).

### 대진표 상세 보기 / 승패 기록
- **라운드 탭**(1R·2R…): 한 화면에 한 라운드만 표시, 기본 1라운드. `viewRound`로 현재 라운드 유지(승 기록/자동 새로고침 후에도 유지). 미기록 채점 라운드 탭에는 주황 점.
- 경기 라벨은 "N번 대진"(옛 "N번 코트"). 각 경기: 앞팀/뒤팀 이름 박스 + 가운데 `VS` 좌우로 동그란 **"승"** 버튼(누른 팀 이름 박스 초록 강조).
- **승 기록은 누구나** 가능(운영진 아님). `POST /api/posts/:id/result {key,winner}` — payload.results만 병합, version 낙관적 동시성(충돌 시 재시도). 랜덤 라운드엔 승 버튼 없음("점수 미반영" 안내).
- **자동 새로고침(폴링)**: 미정산 대진표를 보는 동안 5초마다 version 확인, 바뀌면 화면 갱신(보던 라운드 유지). 편집/다이얼로그/정산 중엔 멈춤.

### 대진 마감(정산) · MVP · 마감 취소 (운영진)
- 모든 채점 라운드의 모든 경기에 승이 기록되면 상세에 **"대진 마감"** 버튼 등장(미기록이 있으면 대신 "어느 라운드가 남았는지" 안내). 누르면 **예/아니오 확인창**("정말 이 대진을 마감할까요? … (마감 후에도 되돌릴 수 있어요.)").
- 마감(정산, `POST /api/posts/:id/settle`, 운영진): 출석+1(참가자 1회)·승+1·패-1을 `ranking_members`에 누적, 순위/시드 재계산, `ranking_events`·`ranking_settlements` 기록. **랜덤 라운드는 점수에서 제외.** payload에 `settledAt`, `mvp`, `preTitle` 저장.
- **MVP**: 채점(비랜덤) 경기를 하나 이상 하고 **하나도 안 진 회원**(게스트 제외). 있으면 대진표 제목을 "M월 D일 정모의 MVP A · B"로 바꾸고, 홈에 작은 반짝이는 배너로 **정산일 기준 5일간** 표시(`GET /api/mvp` = 최근 정산 대진표의 mvp).
- **마감 취소**(`POST /api/posts/:id/unsettle`, 운영진, **가장 최근 마감본만**): `ranking_events` 델타를 역산해 점수·출석·승·패 원복, 순위/시드 재계산, `settledAt`·`mvp` 해제·제목(`preTitle`) 복원, 해당 events·settlements 삭제. 정산된 대진표 상세에 '마감 취소' 버튼.
- **주간 초기화(요청 047)**: `attendance/wins/losses`는 **새 대진표를 만들 때(version 0 저장) 0으로 초기화**된다(그 대진의 성적만 표시). **`points`·`seed`·`rank`는 계속 누적.** 즉 콕끼리는 수·토 정모마다 새 대진을 만들면 출석/승/패는 그 회차 값, 시드 점수는 시즌 누적.

### 시드현황(랭킹) 표
- 회원 랭킹(ranking_members)과 게스트(roster의 게스트, 점수는 2026-09-10 기준표, 누적 미반영) 두 탭, 8열(순위·변동·이름·시드·점수·출석·승·패).
- **순위 변동 화살표**: 상승 빨강 ▲(#e5484d), 하향 파랑 ▼(#2f6fed). (직전 정산 대비.)
- **운영진 왕관 👑**: 시드현황 표에만 표시(로토·백구·구구·이코·뉴키·단우 6명). 대진표·참가자 선택에는 표시하지 않음(단, 쉬는 순서용 운영진 판정 로직은 그대로).
- **급수 +/-**: `A+` → `A` 뒤에 작은 위첨자로.
- **이름 정리**: 명단의 `(서울)`·`(부재)` 표기는 제거. 단 **이름이 겹치는 경우만 지역명을 남겨 작은 회색 글씨**로 표시(현재: 게스트 `철(서울)`↔회원 `철`, 게스트 `선호(서울)`↔게스트 `선호`). 렌더는 클라이언트 `nameHTML`이 이름 끝 "(지역)"을 회색 `.region`으로.

### 목록(게시판)
- 대진표·공지 목록. **마감된 대진표는 제목 뒤에 작은 회색 "마감" 태그**(`.settled-tag`). 목록 API `/api/posts`가 `settledAt`을 함께 반환.

---

## 6. API 엔드포인트 요약 (`boards.js`)

| 메서드·경로 | 권한 | 설명 |
| --- | --- | --- |
| `GET /api/people` | 공개 | 회원+게스트 명단(회원 points·seed는 ranking_members에서 병합). |
| `GET /api/rankings` | 공개 | 회원 랭킹 + sourceDate/updatedDate(최근 정산일). |
| `GET /api/mvp` | 공개 | 최근 정산 대진표의 mvp·settledAt(홈 배너용). |
| `GET /api/posts?kind=` | 공개 | 목록(title·settledAt·created_at·version). |
| `GET /api/posts/:id` | 공개 | 게시글 1건(unpack). |
| `PUT /api/posts/:id` | 운영진 | 생성/수정(version). schedule 생성(version 0) 시 출석/승/패 0 초기화. |
| `DELETE /api/posts/:id` | 운영진 | 삭제(정산 완료 대진표는 409). |
| `POST /api/posts/:id/result` | 공개 | 승패 1건 기록(랜덤·정산완료 거부, version 낙관적). |
| `POST /api/posts/:id/settle` | 운영진 | 마감(정산)·MVP. |
| `POST /api/posts/:id/unsettle` | 운영진 | 마감 취소(최근 마감본만). |
| `POST /api/operator-login` | 공개 | 운영진 로그인(비번→비밀 주소 redirect). |

---

## 7. 지금까지의 작업 흐름 (요약 — 상세는 메인.md)

- **008(별도 Codex)**: 홈페이지 최초 구축(공지·대진표·시드현황, 명단, 디자인).
- **011~018**: 운영진권한 버튼, 공지 표시, 홈 카드, 하단 "함께한지 N일", 출석·승패 누적·시드 자동 반영, 기록 규칙, 최신 시드표 기준.
- **025~028**: 옛 Codex Sites → **GitHub + Cloudflare Workers + D1**로 이전, 주소에서 개인 아이디 제거(하위도메인 kokkiri-badminton), 계정 설정 대행, 비밀값 등록.
- **029~034**: 공지 표시 개선, 게스트 탭 표 통일, 대진표 상세 디자인, 시드현황 최종수정일자, 통일성·접근성·파비콘.
- **035~052(주로 Claude/Opus)**: 즉석 게스트 추가, 대진표 삭제, **라운드별 매칭 엔진·왕관·늦참**, 승 슬라이더→동그란 승 버튼(VS 좌우), 회원도 승 기록, **대진 마감·MVP**, 랜덤 경기 승 제외, **홈 MVP 배너(5일)**, 명단 (서울)/(부재) 정리·겹침시 지역 회색 표기, 급수 첨자, **출석/승/패 주간 초기화(점수·순위 누적)**, 순위 화살표 빨강/파랑, **라운드별 탭 보기**, **5초 자동 새로고침**, 코트→대진 라벨, 미기록 라운드 안내.
- **053~054**: '확정'→'마감' 문구, **마감 취소(unsettle)**, 목록 마감 태그.
- **055~058(Codex)**: 모델 분담 규칙 문서화(056) 등 — **정확한 내용은 [`메인.md`](메인.md) 요청 이력과 [`기록/`](기록/) 최신 파일 참고.**

> 이 문서는 035~054 기준으로 상세하다. 그 이후(055+) Codex 변경은 메인.md/기록이 기준이며, 실제 코드가 최종 진실이다.

---

## 8. 새 작업 시작 체크리스트

1. `git pull --ff-only origin main` (다른 세션 변경 반영).
2. [`메인.md`](메인.md) '요청 이력'·'현재 상태와 미완료 사항' 읽기. 새 요청은 다음 번호로 추가하고 '진행 중' 표기.
3. 코드 수정은 `dist/server/*.js`. 클라이언트는 인라인 제약(§1-1) 지키기.
4. `node check-login.mjs` PASS. 필요하면 `--serve`로 화면 확인. 승패/정산/랭킹 같은 위험 로직은 로컬 e2e 권장.
5. commit(짧은 ASCII 메시지) → `git push origin main` → 공개 주소에서 배포 확인.
6. [`기록/`](기록/)에 상세 기록 작성, [`메인.md`](메인.md) 상태·최근 갱신 시각 갱신.
7. 비밀값·개인정보 노출 금지. 실제 운영 데이터로 테스트 정산/마감 금지(정산은 랭킹을 바꾼다).
