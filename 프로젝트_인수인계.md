# 콕끼리 배드민턴 홈페이지 — 프로젝트 인수인계 (다른 AI/작업자용)

> 이 문서 하나로 프로젝트 전체를 이해하고 바로 이어서 작업할 수 있게 정리한 기술 인수인계서다.
> - 요청(사용자 지시) 이력과 상태: [`메인.md`](메인.md) — 단일 기준 문서, 새 요청은 여기에 번호로 추가.
> - 요청별 상세 작업 기록: [`기록/`](기록/) 폴더(YYYY-MM-DD_HH-mm-ss_핵심내용.md).
> - 작업 방식 규칙: [`CLAUDE.md`](CLAUDE.md)(Claude 세션), [`AGENTS.md`](AGENTS.md)(Codex 세션).
> - 이 인수인계서는 "시스템이 어떻게 돌아가는가"를 설명한다(요청 로그는 메인.md 참고).
> - 최초 작성: 2026-09-13 KST(요청 059). **갱신: 2026-09-15 요청 091** — 회원·게스트는 D1로 관리하며, 주간 성적 표시는 새 대진 생성이 아니라 최신 마감 정산 이력으로 결정한다. 현재 초기화/복구 규칙은 §10이 이전 본문보다 우선한다. 이후 큰 구조 변경이 있으면 이 문서도 갱신할 것.

---

## 0. 한 줄 요약

배드민턴 모임 "콕끼리"의 회원용 홈페이지. **공지사항 · 대진표(경기 편성/승패 기록/점수 정산) · 시드현황(회원·게스트 랭킹)** 을 제공한다. Cloudflare Workers + D1(SQLite)로 돌아가고, GitHub `main`에 push하면 자동 배포된다.

- **공개 주소**: https://kokkiri.kokkiri-badminton.workers.dev
- **저장소**: GitHub `rkdtjdwns1312-wq/-` (origin), 폴더 `kokkiri-site/`.

---

## 1. 작업 환경과 배포 (먼저 읽기)

- **빌드 도구 없음**: `dist/server/*.js` 파일이 곧 배포물이다(번들러/트랜스파일 없음). 파일을 고치고 push하면 그대로 돈다.
- **git·node가 PATH에 없다.** Codex 번들을 쓴다:
  - git: `C:\Users\just\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\git\cmd\git.exe`
  - node(v24): `C:\Users\just\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe`
- **작업 절차**: `git pull --ff-only origin main` → 코드 수정 → `node check-login.mjs`(PASS 확인) → `git commit` → `git push origin main` → Cloudflare Workers Builds가 `wrangler d1 migrations apply DB --remote && wrangler deploy` 자동 실행.
- **배포 확인**: 공개 주소나 Cloudflare 대시보드(Workers & Pages → kokkiri → Deployments). 확인 전에는 "배포 완료"라고 적지 않는다.
- **테스트**: `check-login.mjs`(node:sqlite 인메모리 DB로 서버를 그대로 구동). 새 마이그레이션을 추가하면 `check-login.mjs` 상단의 마이그레이션 목록에도 넣는다. `node check-login.mjs --serve`로 http://localhost:4173 로컬 미리보기(운영진: `/operate-test-editor-key`, 비번 `test-password`).
- **협업 주의(중요)**: Claude와 Codex가 **같은 워킹트리**를 공유한다. 동시에 작업하면 커밋이 겹치거나 서로의 미커밋 변경을 함께 커밋할 수 있다. **한 번에 한 도구로**, 작업 전 pull, 작업 후 곧바로 commit·push. 강제 push·hard reset 금지, 로컬 변경 보존.
- **PowerShell 커밋 팁**: 커밋 메시지에 `→ … ? "` 같은 문자를 here-string에 넣으면 파싱이 깨진다. ASCII 위주의 간단한 메시지를 쓴다.

## 1-1. 절대 건드리면 안 되는 것 / 제약

- `wrangler.jsonc`의 **`"keep_names": false`** — 지우면 회원 화면이 빈 화면이 된다(esbuild `__name()` 헬퍼가 인라인 클라이언트를 깨뜨림). 배포 후 `__name`이 페이지 소스에 0개여야 정상.
- **클라이언트는 `client(EDITOR)` 함수 하나를 통째로 문자열로 인라인**한다(`page()`에서 `client.toString()`). 따라서 이 함수 안에서는 **바깥 스코프(모듈 변수/함수)를 참조하면 안 된다** — 런타임 ReferenceError. 필요한 건 함수 안에 넣거나 인자로 넘긴다. (테스트의 `new Function('return ('+client.toString()+')')`는 문법만 검사하고 참조 오류는 못 잡으니 주의.)
- **비밀값 금지**: 운영진 비밀번호, 운영진 비밀 주소(`/operate-<EDITOR_KEY>`), `EDITOR_KEY`·`OPERATOR_PASSWORD` 값, 토큰을 md·소스·주석에 남기지 않는다. 이 값들은 Cloudflare 대시보드 Secrets에 사용자가 직접 넣는다.
- 이미지는 `public/` 정적 자산으로 제공(무료 플랜 번들 3MB 제한 때문에 서버 코드에 큰 파일 내장 금지). `dist/server/images.js`는 빈 스텁.
- 줄바꿈은 `.gitattributes`로 LF 통일.
- 프로젝트 루트(`배드민턴 프로그램 만들기/`)의 `index.html`·`app.js`·`firebase.json` 등은 2026-09-10 첫 Firebase 시도 잔재로 현재 홈페이지와 무관. 삭제하지 말고 방치.

---

## 2. 파일 구조 (`dist/server/`)

| 파일 | 역할 |
| --- | --- |
| `index.js` | Worker 진입점. `boards.js`의 default export(`fetch`·주간 백업 `scheduled`)를 그대로 재-export한다. |
| `boards.js` | **서버 핵심**: 라우팅, HTML 페이지 렌더(`page()`), 스케줄 검증(`validate`), 정산(`settleSchedule`), 마감 취소(`unsettleSchedule`), API 엔드포인트. |
| `boards-client.js` | **회원/운영진/관리자 화면(SPA)** — `client(EDITOR, ADMIN, createScheduleTools)`. 해시 라우팅, 대진 편성 picker, 승패 기록, 시드 표. `client.toString()`으로 인라인되고 세 번째 인자로 직렬화된 도구 factory를 받는다. |
| `schedule-tools.js` | 요청 084~087: 외부 의존성 없는 `createScheduleTools()` factory. generate(늦참·휴식·팀 균형), availableNames, refreshRound, replacePlayer. 서버와 브라우저가 공유하며 factory 내부에서 모듈 외부 함수를 참조하면 안 된다. |
| `boards-style.js` | 전체 CSS(`export const css`). 일부 표 스타일은 boards-client.js가 런타임에 `document.head`에 주입하는 `extraStyle`에도 있다(순위 화살표 색 등은 여기서 이김 — 겹치는 규칙 수정 시 주의). |
| `rankings.js` | 시드 계산 규칙 `seedForPoints`(점수→급수 구간)와 `orderRankingRows`(점수순 재정렬·순위/시드 재계산). 운영 명단은 코드에 두지 않는다. |
| `operator-login.js` | 운영진 로그인 처리(`/api/operator-login`), 시도 횟수 제한. |
| `admin-login.js` | 홈페이지 관리자 로그인 처리(`/api/admin-login`)와 `EDITOR_KEY`·`ADMIN_PASSWORD` 기반 관리자 키 파생. |
| `images.js` | 빈 스텁(이미지는 public/). |
| `roster-ui.js` | (레거시로 추정) 옛 명단 UI. 현재 메인 흐름은 boards-client.js. 새로 참조하지 말 것. |

- 마이그레이션: `drizzle/0000~0012*.sql`. `check-login.mjs`에 목록이 있으니 새 파일 추가 시 함께 갱신. (0006=hidden+guests, 0007=정모, 0008=legacy 정리+previous_points, 0009=guests 정산 컬럼+guest_events, 0010=ranking_members.is_operator+backups, 0011=순위변동·20점 보호, 0012=초기 명단 DB 등록·정보 수정 이력.)
- 설정: `wrangler.jsonc`(Worker 이름 kokkiri, D1 바인딩 DB, migrations_dir drizzle, assets public, keep_names:false, workers_dev:true).

---

## 3. 데이터 모델 (D1 / SQLite)

- **`schedules`**: 옛 단일 대진표(id=1). 마이그레이션 0004가 이걸 `board_posts`의 `legacy-schedule`로 1회 이관했다. (예전엔 조회마다 되살리는 `preserveLegacy`가 있었으나, 삭제해도 다시 생기는 버그라 요청 065에서 제거하고 0008에서 legacy-schedule를 삭제했다.)
- **`board_posts`**: 공지+대진표 게시글. `(id, kind['notice'|'schedule'], payload(JSON), version, last_operation, created_at, updated_at)`. 낙관적 동시성은 `version`으로.
- **`ranking_members`**: 회원 랭킹 상태 `(member_id, name, points, seed, rank, previous_rank, previous_points, attendance, wins, losses, updated_at, hidden, is_operator, rank_movement, rank_protected, floor_protected_at, promoted_guest_id, edit_version)`. 최초 61명은 마이그레이션 0012에서 한 번만 등록하며 이후 요청마다 코드가 시드하지 않는다. **`hidden`(0006, 기본 0)**: 1이면 삭제된 회원 — 모든 조회는 `WHERE hidden=0`, 삭제 시 생존자 순위 재정렬. **`previous_points`(0008)**: 순위의 `previous_rank`처럼 점수 변동 화살표용 — 정산 직전 점수를 담고, 새 대진 시작(version 0) 시 `previous_points=points`로 초기화. **`edit_version`(0012)**: 운영진 수동 수정의 동시 저장 충돌 방지용 버전.
- **`guests`**: 게스트 명단 `(guest_id, name, points, previous_points, attendance, wins, losses, hidden, created_at, rank_protected, floor_protected_at, edit_version)`. 최초 기준 게스트 32명은 마이그레이션 0012에서 한 번만 등록하고, 시드는 `seedForPoints(points)`로 계산한다. `hidden=1`이면 삭제. **게스트도 정산 점수 변동 대상(068)**: 회원과 같은 규칙(출석 +1·승 +1·패 -1, 점수 누적, previous_points 화살표, 새 대진 시작 시 초기화). 단 **MVP는 회원만**(039 유지).
- **`people_changes`**: 운영진의 사람 추가·수정·숨김·이관 이력 `(id, person_type, person_id, action, before_name, after_name, before_points, after_points, reason, created_at)`. 이름 수정은 기존 대진 payload를 소급 변경하지 않는다. 점수 수동 수정은 정산 이벤트를 삭제하지 않으며 정산 화살표를 새 점수 기준으로 초기화한다.
- **`guest_events`**: 게스트 정산 델타 `(schedule_id, guest_id, attendance_points, win_points, loss_points, total_points, points_before, points_after, created_at)`. ranking_events의 게스트판 — 마감 취소가 역산에 사용(068).
- **`ranking_settlements`**: 정산 1건당 1행 `(schedule_id, settled_at, operation)`.
- **`ranking_events`**: 정산 시 회원별 델타 기록 `(schedule_id, member_id, attendance_points, win_points, loss_points, total_points, points_before/after, rank_before/after, seed_before/after, created_at)`. **마감 취소가 이 델타를 역산**해 되돌린다.
- **`operator_login_limits`**: 로그인 시도 제한.

### 대진표 payload(JSON) 형태
```
{
  title, names[], participantIds[],  // names[i] ↔ participantIds[i] 같은 순서(정렬 매칭에 중요)
  courts, rounds,
  schedule: [ { round, method['same'|'balanced'|'random'], g:[[a1,a2,b1,b2],...], rest:[...], late:[...] }, ... ],
  lateRounds: { 표시이름: 1~5 }, // 앞 N개 라운드 제외(미지정 0)
  lateRegistration: [name,...], // 신청늦음: 휴식 1순위, 도착 시간과 별도
  results: { "<roundIndex>-<courtIndex>": "a"|"b", ... },
  settledAt: null | ISO,   // 정산(마감) 시각
  mvp: [name,...],         // 정산 시 채워짐(회원만, 아래 MVP 규칙)
  preTitle: 원제목,         // 정산 시 저장 → 마감 취소가 제목 복원에 사용
  absent: [name,...]       // 불참자(061). 이 이름이 낀 경기는 무효(정산·승패 미반영), 본인은 출석 미부여
}
```
- 서버 `validate()`가 payload를 화이트리스트로 정리한다. `method`, `lateRounds`, `lateRegistration`, `mvp`, `absent`는 저장된다(084). 미도착자가 경기에 있으면 저장 거절, `rest`는 도착자 중 출전하지 않는 사람, `late`는 미도착자를 서버에서 계산한다. 전체 라운드 종료까지 늦참인 사람은 출석 점수도 받지 않는다.
- **라운드 경기 수는 가변**(062): `g` 길이는 보통 1~100이며 도착자가 4명 미만일 때만 0 허용(084). `courts`와 일치할 필요 없다. 같은 라운드 다른 경기에 중복 출전 허용, 한 경기 안에서는 4명 중복 금지. `rest`·`late`·`results`는 서버가 다시 계산·정리한다.

---

## 4. 인증/권한 모델 (중요한 설계 전제)

- **회원 로그인이 없다.** 운영진 인증은 두 경로다: 비밀 주소 `/operate-<EDITOR_KEY>` 로 들어오거나, 회원 화면 오른쪽 아래 **'운영진권한'** 버튼+`OPERATOR_PASSWORD`로 로그인한다. 로그인 성공 시 `EDITOR`(=키)가 클라이언트에 주입되고, 저장·정산 등에 `x-kokkiri-editor` 헤더로 쓰인다.
- 따라서 **"회원"은 사실상 "링크를 가진 누구나"** 이다. 승패 기록(`/result`)은 이 전제로 **누구나 정산 전까지 기록 가능**하게 열려 있다(요청 037). 최종 확정(마감/정산)과 삭제·마감취소는 운영진 전용.
- 점수 규칙: **정모 출석 +1(모임당 1회), 승 +1, 패 -1**. 점수 누계로 급수(시드) 자동 계산(`seedForPoints` 구간). **랜덤 라운드는 승패를 점수에 반영하지 않는다.**
- **홈페이지 관리자(073·074)**: 운영진보다 상위 권한. 회원 홈 '홈페이지관리자 권한' 버튼 → 관리자 비밀번호(`ADMIN_PASSWORD` 비밀값, 사용자가 Cloudflare에 등록) → 관리자 비밀 주소 `/administrate-<파생키>`. 관리자 비밀 주소 키는 `EDITOR_KEY+ADMIN_PASSWORD`에서 서버가 SHA-256으로 파생(`admin-login.js` adminKeyFor) → 새 비밀값은 ADMIN_PASSWORD 하나. 관리자 모드는 운영진 권한 포함. 관리자 전용 동작은 `x-kokkiri-admin`(=파생키) 헤더로 검증: **운영진 임명/해제**(왕관, ranking_members.is_operator 토글), **백업**(공지·대진·시드 스냅샷, 주간 자동+수동, 1개월 보관). 왕관은 이제 is_operator 기반(클라 `OPERATORS`를 데이터로 동기화).

---

## 5. 주요 화면과 기능 (동작 방식)

### 사람 관리(시드현황 '수정하기', 운영진, 060)
- 시드현황(랭킹) 화면에서 운영진에게 **'수정하기'** 버튼 → 편집 모드(추가하기 + 행 체크박스 삭제), 다시 누르면 '완료'.
- **추가하기**: 다이얼로그에서 닉네임·시드점수(0~1000)·회원/게스트 선택 → `POST /api/people`. 이름 중복 409. 회원 추가 시 점수로 순위 재계산.
- **삭제**: 체크한 사람들 `POST /api/people/hide`(회원=ranking_members.hidden=1, 게스트=guests.hidden=1). 회원 삭제 후 생존자 순위 재정렬(빈 순위 없음).
- **정보 수정**: 운영진 수정 모드에서 사람별 `수정` 버튼 → 이름·점수(20~1000)·필수 사유 입력 → `POST /api/people/update`. 점수 수동 수정은 정산 이벤트를 건드리지 않고 해당 점수의 화살표를 초기화한다. 기존 대진표 이름은 소급 변경하지 않는다.
- **변경 이력**: 사람별 `이력` 버튼 → `GET /api/people/history`로 추가·수정·이관 기록을 최근 50건까지 운영진에게 표시한다. `edit_version` 낙관적 잠금으로 다른 운영진이 먼저 저장한 경우 409를 반환한다.
- 변경은 **대진 만들기 선택 목록·인원수에 즉시 반영**된다: 클라 `people` 캐시를 비워(`people=[]`) 다음 picker 열 때 `/api/people`를 다시 부른다.
- 이 관리 UI가 생기기 전 쓰던 picker의 '즉석 게스트 추가'(임시 uuid, 이 대진에만)는 그대로 남아 있다. 계속 올 게스트는 시드현황 추가가 정식 경로.

### 대진 편성(운영진, `openPicker`, `generate`)
- 코트 수·라운드 수, **라운드별 매칭 방식** 선택(086 재정의):
  - **랜덤**: 완전 랜덤으로 4명씩 코트 배정, 팀도 무작위.
  - **동일**: 점수 내림차순으로 가까운 4명씩 묶고 `1·3 vs 2·4` 또는 `1·4 vs 2·3` 중 팀 평균 차이가 가장 작은 조합 선택.
  - **인접**: 8명 블록마다 `1·2·5·6`, `3·4·7·8`로 묶고 동일한 팀 균형 기준 적용(예: `1·6 vs 2·5`, `3·8 vs 4·7`). 남은 4명은 가까운 4명으로 구성. 여기의 간격은 알파벳 급수 4단계가 아니라 사용자가 제시한 점수순 명단 위치 예시를 따른다.
- 파트너 중복은 팀 점수 차이가 같은 경우 최소화(071을 086의 균형 우선에 맞춤). 랜덤은 점수를 사용하지 않으며 동료 중복 횟수를 줄인다.
- **휴식(085)**: 신청늦음→게스트→운영진 순으로 먼저 배정하고 이 우선대상은 전체 정모에서 최대 1회. 이후 일반 회원 중 덜 쉰 사람부터 무작위 배정. 사용자 확답으로 일반 회원은 필요시 추가 휴식 허용. 아직 미도착한 라운드는 휴식 횟수에 포함하지 않는다. 우선대상만 남아 1회 제한을 지키며 채울 수 없으면 코트/참가자/라운드 조정 안내를 띄운다.
- 참가자 체크(회원/게스트 탭). **급히 온 게스트는 "추가하기"로 즉석 추가**(임시, 이 대진에만; 랭킹/시드표 미반영, id는 임시 uuid).
- **늦참(084)**: 버튼 펼침 → 1~5 선택(재클릭하면 해제). 2이면 1·2R 대진·휴식에서 제외, 3R부터 배정 후보. 신청늦음은 독립 버튼으로 도착이 늦지 않아도 지정 가능. 저장·다시 편집·참가자/코트 변경에도 방식과 늦참 설정을 복원한다.
- 저장 시 `names[]`와 `participantIds[]`를 같은 순서로 만든다(정산에서 이름↔회원 매핑에 사용).

### 대진표 상세 보기 / 승패 기록
- **라운드 탭**(1R·2R…): 한 화면에 한 라운드만 표시, 기본 1라운드. `viewRound`로 현재 라운드 유지(승 기록/자동 새로고침 후에도 유지). 미기록 채점 라운드 탭에는 주황 점.
- 경기 라벨은 "N번 대진"(옛 "N번 코트"). 각 경기: 앞팀/뒤팀 이름 박스 + 가운데 `VS` 좌우로 동그란 **"승"** 버튼(누른 팀 이름 박스 초록 강조).
- **승 기록은 누구나** 가능(운영진 아님). `POST /api/posts/:id/result {key,winner}` — payload.results만 병합, version 낙관적 동시성(충돌 시 재시도). 랜덤 라운드엔 승 버튼 없음("점수 미반영" 안내).
- **자동 새로고침(폴링)**: 미정산 대진표를 보는 동안 5초마다 version 확인, 바뀌면 화면 갱신(보던 라운드 유지). 편집/다이얼로그/정산 중엔 멈춤.

### 대진 수정 편집 모드(운영진, `editSchedule`) — 불참 · 코트 추가/삭제 (061·062·064)
- 상세 화면 '수정하기' → `#editMatches`에 편집용 대진을 그린다. 요청 087부터 **선택한 자리만 변경**, 다른 자리와 자동 스왑하지 않는다. 같은 라운드 다른 경기 중복 출전 허용, 같은 경기 중복·미도착자는 거절. 변경된 경기의 승패만 지우고 다른 경기 결과는 보존하며 휴식을 재계산한다. 저장은 `PUT /api/posts/:id`.
- **작성 화면 공백 원인(086)**: 기본 220px 카드에서 가운데 승패 버튼이 공간을 차지해 이름 select가 16px까지 축소됨. 편집용 넓은 grid·이름 칸 최소 너비 처리, 승 버튼을 각 팀 아래로 배치해 해결. 실제 코드가 생성되지 않던 문제가 아니었다.
- **불참자 지정 토글**: 상단 '불참자 지정' 패널의 이름 칩(`.absent-toggle`)을 누르면 `draft.absent`에 추가/삭제된다. 불참자가 낀 경기는 그 자리에서 **무효**(승 버튼 사라지고 "무효 경기" 안내). 정산·`/result`가 무효 경기를 제외하고, 불참자 본인은 출석 미부여.
- **코트 추가(＋)**: 라운드마다 `.add-court` 버튼. 누르면 그 라운드에 경기 1개 추가(쉬는 사람 우선, 없으면 명단 순으로 서로 다른 4명 자동 배치 → 드롭다운으로 조정). **대체 경기·한 라운드 두 번 뛰기**가 이렇게 구현된다.
- **코트 삭제(✕)**: 경기의 `.court` 안 `.del-court` 버튼. 확인 후 그 경기를 지우고, **같은 라운드의 승패 결과 키(`ri-mi`)를 재인덱싱**(지운 mi보다 큰 것은 1씩 당김)해 결과가 엉키지 않게 한다. 한 라운드 최소 1개 경기는 유지(가드).

### 대진 마감(정산) · MVP · 마감 취소 (운영진)
- 모든 채점 라운드의 모든 경기에 승이 기록되면 상세에 **"대진 마감"** 버튼 등장(미기록이 있으면 대신 "어느 라운드가 남았는지" 안내). 누르면 **예/아니오 확인창**("정말 이 대진을 마감할까요? … (마감 후에도 되돌릴 수 있어요.)").
- 마감(정산, `POST /api/posts/:id/settle`, 운영진): 출석+1(참가자 1회)·승+1·패-1을 `ranking_members`에 누적, 순위/시드 재계산, `ranking_events`·`ranking_settlements` 기록. **랜덤 라운드와 무효 경기(`absent` 참가자가 낀 경기)는 점수에서 제외**, **불참자는 출석도 미부여.** payload에 `settledAt`, `mvp`, `preTitle` 저장.
- **MVP**: 채점(비랜덤) 경기를 하나 이상 하고 **하나도 안 진 회원**(게스트 제외). **마감해도 제목은 원제목 그대로 두고 mvp[]에만 저장(078)** — 요청 083으로 **대진표 목록** 제목 아래 작은 반짝이는 줄(`🥇 MVP 이름들`, 이름 공백 구분)로 이동했으며 상세에서는 표시하지 않는다. 목록은 마감된 글에 MVP가 있으면 표시하고 5일 제한은 없다. 홈은 기존 작은 반짝이는 배너(세로 2줄, 🥇 이번 정모 MVP, 077)를 **정산일 기준 5일간** 표시(`GET /api/mvp`). 목록/상세 제목은 `preTitle` 우선(COALESCE)이라 예전 방식으로 제목이 바뀐 글도 원제목으로 보인다.
- **마감 취소**(`POST /api/posts/:id/unsettle`, 운영진, **가장 최근 마감본만**): `ranking_events` 델타를 역산해 점수·출석·승·패 원복, 순위/시드 재계산, `settledAt`·`mvp` 해제·제목(`preTitle`) 복원, 해당 events·settlements 삭제. 정산된 대진표 상세에 '마감 취소' 버튼.
- **주간 초기화(요청 047·066)**: `attendance/wins/losses`는 **새 대진표를 만들 때(version 0 저장) 0으로 초기화**된다(그 대진의 성적만 표시). 같은 시점에 **순위·점수 변동 화살표도 초기화**(`previous_rank=rank`, `previous_points=points`)되어 사라진다. **`points`·`seed`·`rank`(누계 값)는 계속 누적.** 즉 새 대진을 만들면 출석/승/패는 0, 화살표는 없음 → 그 대진을 마감하면 다시 변동이 표시된다.

### 시드현황(랭킹) 표
- 회원 랭킹(`ranking_members`)과 게스트(`guests`) 두 탭, 8열(순위·변동·이름·시드·점수·출석·승·패). 게스트는 최초 명단을 DB에 시드한 뒤 DB가 기준이며, 정산 시 점수가 누적되고 점수 변동도 표시된다. 출석·승·패는 회원과 같이 새 대진 생성 시 0으로 초기화된다.
- **순위 변동 화살표**('변동' 열): 상승 빨강 ▲(#e5484d), 하향 파랑 ▼(#2f6fed). 정산 때 저장한 `rank_movement` 기준(명단 이관/추가/삭제 뒤에도 유지). 최근 정모 유효 비랜덤 경기의 승패 대상만 표시한다. 변동 없으면 '-', 보호 대상은 작은 '보호적용'(§9).
- **점수 변동 화살표(066)**: '점수' 열에서 점수 옆에 작게 ▲빨강/▼파랑+수치. `previous_points` 대비(마감으로 반영된 증감). 같은 색·규칙을 순위와 공유(`.rank-movement`/`.rank-down`), 클라 `pointsMove(row)`. 변동 없으면 표시 안 함.
- **운영진 왕관 👑**: `ranking_members.is_operator`를 기준으로 동적으로 표시한다. 마이그레이션 0010은 기존 운영진 6명을 초기값으로 넣었지만, 홈페이지 관리자 화면에서 임명·해제할 수 있다. 대진표·참가자 선택에는 표시하지 않음(단, 쉬는 순서용 운영진 판정 로직은 그대로).
- **급수 +/-**: `A+` → `A` 뒤에 작은 위첨자로.
- **이름 정리**: 명단의 `(서울)`·`(부재)` 표기는 제거. 단 **이름이 겹치는 경우만 지역명을 남겨 작은 회색 글씨**로 표시(현재: 게스트 `철(서울)`↔회원 `철`, 게스트 `선호(서울)`↔게스트 `선호`). 렌더는 클라이언트 `nameHTML`이 이름 끝 "(지역)"을 회색 `.region`으로.

### 목록(게시판)
- 대진표·공지 목록. **마감된 대진표는 제목 뒤에 작은 회색 "마감" 태그**(`.settled-tag`), MVP가 있으면 제목 아래 작은 MVP 줄 표시(083). 목록 API `/api/posts`가 `settledAt`과 `mvp` 배열(없으면 빈 배열)을 함께 반환한다. 마감 취소 시 목록의 MVP도 사라진다.

---

## 6. API 엔드포인트 요약 (`boards.js`)

| 메서드·경로 | 권한 | 설명 |
| --- | --- | --- |
| `GET /api/people` | 공개 | 회원(ranking_members, hidden=0)+게스트(guests, hidden=0) 명단. |
| `POST /api/people` | 운영진 | 회원/게스트 추가(닉네임·시드점수). 이름 중복 409, 회원 추가 시 순위 재계산. |
| `POST /api/people/update` | 운영진 | 회원/게스트 이름·점수 수정. 수정 사유 저장, `edit_version` 충돌 보호, 점수 수동 수정은 기존 정산 이벤트를 보존. |
| `GET /api/people/history` | 운영진 | 사람별 추가·수정·이관 변경 이력 최근 50건. 회원 이관 전 게스트 이력도 연결해 표시. |
| `POST /api/people/hide` | 운영진 | 선택 사람 삭제(hidden=1). 회원 삭제 후 생존자 순위 재정렬. |
| `POST /api/people/promote` | 운영진 | 게스트를 회원으로 이관(069). 점수·출석·승·패 그대로 ranking_members로, 게스트 hidden, 순위 재정렬. 이름 중복 409. |
| `POST /api/admin-login` | 공개 | 관리자 로그인(비번→관리자 비밀 주소 redirect, 073). |
| `POST /api/operator-role` | 관리자 | 운영진 임명/해제(is_operator 토글, 073). x-kokkiri-admin 필요. |
| `GET/POST /api/backups`, `GET /api/backups/:id` | 관리자 | 백업 목록·생성·다운로드(074). x-kokkiri-admin 필요. 주간 자동 백업은 `scheduled` 핸들러(Cron). |
| `GET /api/rankings` | 공개 | 회원 랭킹(hidden=0) + sourceDate/updatedDate(최근 정산일). |
| `GET /api/mvp` | 공개 | 최근 정산 대진표의 mvp·settledAt(홈 배너용). |
| `GET /api/posts?kind=` | 공개 | 목록(title·settledAt·mvp 배열·created_at·version). |
| `GET /api/posts/:id` | 공개 | 게시글 1건(unpack). |
| `PUT /api/posts/:id` | 운영진 | 생성/수정(version). schedule 생성(version 0) 시 출석/승/패 0 초기화. |
| `DELETE /api/posts/:id` | 운영진 | 삭제(정산 완료 대진표는 409). |
| `POST /api/posts/:id/result` | 공개 | 승패 1건 기록(랜덤·정산완료 거부, version 낙관적). |
| `POST /api/posts/:id/settle` | 운영진 | 마감(정산)·MVP. |
| `POST /api/posts/:id/unsettle` | 운영진 | 마감 취소(최근 마감본만). |
| `POST /api/operator-login` | 공개 | 운영진 로그인(비번→비밀 주소 redirect). |

---

## 7. 지금까지의 작업 흐름 (요약 — 상세는 메인.md)

- **008(별도 Codex)**: 홈페이지 최초 구축(공지·대진표·시드현황, 명단, 디자인).
- **011~018**: 운영진권한 버튼, 공지 표시, 홈 카드, 하단 "함께한지 N일", 출석·승패 누적·시드 자동 반영, 기록 규칙, 최신 시드표 기준.
- **025~028**: 옛 Codex Sites → **GitHub + Cloudflare Workers + D1**로 이전, 주소에서 개인 아이디 제거(하위도메인 kokkiri-badminton), 계정 설정 대행, 비밀값 등록.
- **029~034**: 공지 표시 개선, 게스트 탭 표 통일, 대진표 상세 디자인, 시드현황 최종수정일자, 통일성·접근성·파비콘.
- **035~052(주로 Claude/Opus)**: 즉석 게스트 추가, 대진표 삭제, **라운드별 매칭 엔진·왕관·늦참**, 승 슬라이더→동그란 승 버튼(VS 좌우), 회원도 승 기록, **대진 마감·MVP**, 랜덤 경기 승 제외, **홈 MVP 배너(5일)**, 명단 (서울)/(부재) 정리·겹침시 지역 회색 표기, 급수 첨자, **출석/승/패 주간 초기화(점수·순위 누적)**, 순위 화살표 빨강/파랑, **라운드별 탭 보기**, **5초 자동 새로고침**, 코트→대진 라벨, 미기록 라운드 안내.
- **053~054**: '확정'→'마감' 문구, **마감 취소(unsettle)**, 목록 마감 태그.
- **055~058**: 모델 분담 규칙 문서화(056), 홈 마스코트 옆 글자 제거·가운데 정렬(058) 등.
- **059**: 이 인수인계 문서 최초 작성.
- **060(Opus/Claude)**: **시드현황 사람 추가/삭제**(회원·게스트) — ranking_members.hidden + guests 테이블(0006), '수정하기' 편집 모드, 대진 만들기 목록 반영.
- **061·062·064**: **불참(무효 경기)**·**코트 추가/삭제** — 서버(validate·정산·/result)와 운영진 편집 UI(불참 토글, ＋/✕). §5의 편집 모드 항목 참고.
- **063**: **9월 12일 토요일 정모** 대진표를 결과까지 게시(마이그레이션 0007, **게시 당시에는 마감 전**). 게스트 아식스·제냐·허니 추가. 게시 당시에는 **운영진이 검토 후 '대진 마감'해야 점수 반영**되는 상태였다.
- **065**: 대진표를 삭제해도 목록에 남던 버그 수정 — 조회마다 옛 대진표를 되살리던 `preserveLegacy` 제거, 0008에서 legacy-schedule 삭제.
- **066**: 시드현황 **점수 변동을 순위처럼 ▲/▼+수치**로 표시(`previous_points`, 0008). 새 대진 시작 시 순위·점수 화살표 모두 초기화.
- **067**: MVP 반짝임 톤다운(흰색 혼합 제거).
- **068**: **게스트도 정산 점수 변동 대상**(출석·승·패·점수 누적·화살표, guests 컬럼+guest_events, 0009). 정산이 참가자를 회원/게스트로 판정해 양쪽 반영. MVP는 회원만.
- **069**: 시드현황 수정 모드 게스트 탭에서 **게스트→회원 이관**(POST /api/people/promote).
- **070·071·072**: 대진 매칭 재정의(랜덤/동일/인접) + 중복 파트너 최소화 + 휴식자 수기 수정(§5 대진 편성 참고).
- **073·074**: **홈페이지 관리자 권한**(운영진 임명/해제=is_operator, 왕관 동적) + **백업**(공지·대진·시드 주간 자동·수동·1개월 보관). ADMIN_PASSWORD 비밀값을 사용자가 Cloudflare에 등록해야 로그인 가능.
- **075**: 회원 홈 로그인 버튼을 작은 사람 실루엣 FAB(👤) 하나로 → 누르면 운영진권한·홈페이지관리자 권한 메뉴.
- **077·078**: MVP 표시 개선 — 홈 배너 세로 2줄·🥇, 마감 시 제목 유지하고 MVP는 제목 아래 작게(이름 공백 구분).

> 이 문서는 035~078 기준으로 상세하다. 그 이후 변경은 메인.md/기록이 기준이며, 실제 코드가 최종 진실이다.
> **코덱스 인수인계(요청 079, 2026-09-13)**: 휴식자 생성 시점 사전 지정 UI는 후속 후보(현재는 생성 후 수기 조정). 게스트 정산은 로컬 테스트 PASS이며 실사용 확인은 남아 있다. 관리자 비밀번호 등록 여부·관리자 로그인·실제 백업 실행 이력은 이번에 확인하지 않았다. 백업 일정은 코드상 매주 일요일 09:00 KST다. 공개 조회에서 9/12 정모 마감 완료·MVP 6명을 확인했으며 운영 데이터 변경은 하지 않았다. 최신 상세 상태는 메인.md 요청 079를 따른다.
> **비밀값 안내**: EDITOR_KEY·OPERATOR_PASSWORD·ADMIN_PASSWORD 는 Cloudflare Secrets에 사용자가 직접 넣는다(값은 문서·코드에 남기지 않는다).

---

## 8. 새 작업 시작 체크리스트

1. `git pull --ff-only origin main` (다른 세션 변경 반영).
2. [`메인.md`](메인.md) '요청 이력'·'현재 상태와 미완료 사항' 읽기. 새 요청은 다음 번호로 추가하고 '진행 중' 표기.
3. 코드 수정은 `dist/server/*.js`. 클라이언트는 인라인 제약(§1-1) 지키기.
4. `node check-login.mjs` PASS. 필요하면 `--serve`로 화면 확인. 승패/정산/랭킹 같은 위험 로직은 로컬 e2e 권장.
5. commit(짧은 ASCII 메시지) → `git push origin main` → 공개 주소에서 배포 확인.
6. [`기록/`](기록/)에 상세 기록 작성, [`메인.md`](메인.md) 상태·최근 갱신 시각 갱신.
7. 비밀값·개인정보 노출 금지. 실제 운영 데이터로 테스트 정산/마감 금지(정산은 랭킹을 바꾼다).

## 9. 순위 화살표 보존과 최저 20점 보호 (요청 080·081)

> 아래와 본문에 남은 '새 대진 시작 시 초기화' 설명은 요청 091로 대체되었다. 현재 규칙과 조회 기준은 §10을 우선한다.

- 마이그레이션은 `0012_db_people_catalog_and_edit_history.sql`까지 있다. 새 회귀 검사 `check-ranking-protection.mjs`는 필수 `check-login.mjs`에서 함께 실행된다.
- `ranking_members.rank_movement`: 최근 정산의 직전 순위 - 직후 순위. 승패가 집계된 회원만 저장하며 명단 재정렬은 이 값을 초기화하지 않는다. 순위 자체는 현재 전체 명단 기준이다. 신규 이관자는 회원 순위의 이전 기준이 없으므로 순위 화살표가 없다.
- `ranking_members`·`guests.rank_protected`: 이번 정모의 작은 보호 문구. 유효 비랜덤 경기에서 승패가 있고 출석+승-패 합산 결과가 **20 이하**면 1. points는 항상 최소 20(E)으로 제한한다. 실제 승·패 횟수는 그대로 기록한다.
- `floor_protected_at`: 20점 그룹의 동점 정렬용. 미보호 사람 먼저, 이전에 보호된 사람, 최근에 보호된 사람 순이며 같은 정산 안에서는 기존 순서 유지. 새 대진 생성은 화살표/보호 문구를 지우지만 이 정렬 기준과 점수는 유지한다. 점수가 20을 넘으면 보호 정렬 기준을 해제한다.
- 사용자 확인에 따라 기존 20점 미만 회원·게스트도 20으로 맞춘다. 신규 입력도 20~1000만 허용한다. 엑셀·roster 및 과거 이벤트는 역사적 원본으로 보존하며 현재 점수는 DB가 기준이다.
- 이벤트의 total_points는 제한 전 계산값이 아닌 **실제로 바뀐 점수**다. 마감 취소는 점수·보호 이전 상태를 되돌린다. ranking_settlements.rank_order_before는 정산 직전 회원 ID 순서의 JSON으로, 보호 때문에 바뀐 동점 순서도 복원한다. 과거 이벤트 취소 시에도 최저 20을 지킨다.
- 신규 이관 시 promoted_guest_id로 게스트 정산과 현재 회원을 연결해, 이관 후 마감 취소도 회원 행에 반영한다. 과거 이관자의 연결은 이름만 보고 추정하지 않는다.
- 0011은 최신 정산 이벤트로 지워진 회원 화살표를 복구한다. 새 대진/마감 취소로 이미 초기화한 행은 제외한다. 정산 미참가·불참·랜덤만 참여한 사람은 화살표 대상이 아니다.

## 10. 최신 확정 정모 기준 표시 (요청 091, 2026-09-15)

- 새 대진 생성·수정·삭제에는 이전 정모 표시를 보존한다. 다음 대진을 마감한 경우에만 출석·승·패 및 순위·점수 변동/보호 문구를 새 마감 결과로 바꾼다. 점수·현재 순위는 계속 누적한다.
- `dist/server/weekly-results.js`: `ranking_settlements`의 `settled_at DESC,rowid DESC`로 가장 최근 남은 마감을 고르고, 동일 정산의 `ranking_events`·`guest_events`에서 표시를 읽는다. 대상이 없으면 주간 수치·화살표는 0이다. API `/api/rankings` 및 게스트 `/api/people`에 적용하며 `displaySettlementId`로 기준을 확인할 수 있다.
- 과거 테스트 대진 생성 때문에 표시용 컬럼이 0이어도 이벤트가 보존되어 있으면 자동으로 표시가 복원된다. 운영 점수를 다시 가산하거나 회원/게스트 명단을 덮어쓰지 않는다. 추가 마이그레이션도 없다.
- 점수 변동은 보호 하한을 고려한 이벤트의 실제 전후 차이다. API `previous_points`는 현재 점수에서 그 차이를 뺀 표시 기준이며 DB 원본 컬럼과 반드시 같지는 않다. **예외**: 정산 뒤 수동으로 점수를 수정한 기록(`people_changes`)이 있고 표시 캐시도 명시적으로 초기화된 경우, 해당 인물의 화살표/보호 초기화는 유지한다(출석·승패는 보존). 순위 화살표는 정산 당시 전후 순위 차이고, 비채점자는 제외한다. 회원 이관은 `promoted_guest_id`로 게스트 이력과 연결하고 회원 순위 화살표는 만들지 않는다.
- `scoredState`의 출석/승패 캐시도 마감 시 해당 정모 값으로 교체한다(누적하지 않음). 마감 취소는 점수 역산을 유지하고, 표시 조회는 직전 남은 정산을 자동 선택한다. 취소 직후 DB 표시 캐시보다 이 모듈의 조회 결과가 기준이다.
- 백업 JSON에 `settlements`, `rankingEvents`, `guestEvents`, `peopleChanges`를 추가해 표시 복원 원본과 수동 수정 예외도 보존한다. 기존 백업의 과거 파일에는 없을 수 있다.
- 필수 `check-login.mjs`가 `check-weekly-results.mjs`를 함께 실행한다. 생성/편집/삭제 보존, 실패 마감, 연속 마감, 재시도, 마감 취소, 초기화 버그 복구, 이관 후 복구, 누적 점수 불변을 검증한다.
- 별도 요청 090은 진단만 했다. 생성기의 `pairCount`는 라운드 간 유지되나 균형(gap)이 중복(repeats)보다 우선이다. 늦참 합류 없이도 동일 팀 반복이 가능하다. 규칙 변경은 아직 하지 않았다.

## 11. 공지 빨간 제목·정모 진행 상태·실시간 자유대진 (093~095)

- 2026-09-15 02:42 KST, `80d4f78` 공개 배포 확인. 자유대진은 빈 초기 상태이며 운영진이 코트를 생성하면 사용할 수 있다. 공개 점수/시드/현재 순위 불변 확인.

- 공지에서 `🔴 제목`은 번호형 큰 제목과 같은 모양의 빨간 제목이다. 번호를 임의로 추가하지 않는다. `nb-section-alert`로 공통 제목 여백/모바일 크기를 공유한다.
- 기존 정모 대진의 `matchProgress`는 게시글 payload에 저장된다. 대기중→대진중(테두리 강조), 승 기록→대진종료(강조 해제). 랜덤은 승패가 없으므로 상태 버튼으로 수동 종료한다. 마감/무효는 잠금. 새로운 대진은 상태를 승계하지 않고, 기존 대진 수정은 같은 라운드·코트·방법·이름 순서인 경기만 상태를 보존한다.
- 공개 `POST /api/posts/:id/progress`는 필수 version으로 충돌을 거절하고 점수에 관여하지 않는다. 마감은 게시글 버전 확보→정산 행 NOT NULL 검증을 동일 D1 batch 안에서 실행하여 진행 상태 변경과 충돌하면 점수까지 모두 취소한다. [D1 batch 원자성 문서](https://developers.cloudflare.com/d1/worker-api/d1-database/).
- 홈은 번호 없이 공지사항→대진표→실시간대진→시드현황이며 실시간대진 설명은 `오늘의 자유대진`이다. 데스크톱 2×2, 모바일 1열.
- `#live`는 정모와 별개인 자유대진이다. 운영진만 코트 1~20개를 생성하고, 회원은 `실시간대진 들어가기`에서 이름과 코트를 골라 완료한다. 코트가 차면 4명씩 순서대로 다음 대진 대기열에 등록한다.
- 자유대진은 팀/VS/승패/정산이 없다. 4명이 찬 코트의 `대진 종료`를 누르면 이전 이름을 지우고 다음 대기 그룹을 올린다. 다음 그룹이 아직 4명 미만이면 해당 빈자리에서 추가 참가를 받는다. 대기 그룹은 번호 순 세로, 그룹 내 이름 4개는 가로로 표시한다.
- 새 모듈 `live-courts.js`(API), `live-client.js`·`live-style.js`(UI), 마이그레이션 `0013_live_courts.sql`. D1 `live_courts` id=1에 현재 courts/queue/version만 저장하며 이력/시드/출석 테이블에 쓰지 않는다. 코트 재생성 시 현재 명단과 대기열을 모두 초기화하므로 확인을 받는다. 새로고침/다른 기기 공유를 위한 임시 저장이며 브라우저 메모리만 쓰는 것은 아니다.
- 공개 `/api/live-courts` GET·POST. POST action은 create(운영진), join/leave/end(공개), version 필수. 동시에 다른 입력이 선행하면 409로 거절, 사용자 확인 후 재시도한다. 공개 참여에는 개인 인증이 없으므로 참가 취소/종료는 신뢰 기반이며 엄격한 본인 제한은 별도 요청이다.
- 회귀: `check-match-progress.mjs`, `check-live-courts.mjs`를 필수 `check-login.mjs`에서 함께 실행한다. 실제 운영 코트/승패로 테스트하지 않는다.

## 12. 실시간대진 운영진 개방·종료 (요청 096)

- `4ddc566`, 2026-09-15 03:06 KST 공개 반영 확인. 회원 닫힘 안내, 운영진 시작 질문/아니오 확인. 실제 공개 세션은 version 0·닫힘·빈 코트/대기열을 유지했다.

- §11의 코트 생성만으로 입장 가능했던 동작은 폐기한다. D1 현재 payload의 `isOpen===true`일 때만 회원이 자유대진을 보고 참가/취소/코트 종료를 할 수 있다. 초기/기존 미설정 값은 닫힘으로 읽으며 추가 마이그레이션 없이 이름/코트는 보존한다.
- 운영진 화면의 같은 위치에서 `실시간대진 열기` ↔ `실시간대진 종료`가 교대한다. 각각 `실시간대진을 시작하시겠습니까?` / `실시간대진을 종료하시겠습니까?` 확인창의 `예`일 때만 처리한다. `아니오`/취소는 변경하지 않는다.
- API action `open`·`close`는 `create`와 함께 운영진 헤더 인증 필수다. 전체 종료는 명단 삭제가 아닌 입장·변경 차단이다. 코트 재생성은 기존처럼 명단/대기열 초기화이며 `isOpen`은 그대로 유지한다. 개별 코트의 `end`와 전체 `close`는 다르다.
- 닫힌 상태의 공개 GET은 `courts=[]`, `queue=[]`만 반환한다. 운영진 헤더가 있는 GET은 보존된 전체 현재 코트를 반환한다. 닫힌 동안 join/leave/end는 운영진을 포함해 거절한다(준비용 create와 열기/닫기만 허용).
- 개방/종료와 회원 입력은 같은 version CAS를 사용한다. 먼저 종료됐다면 옛 화면·옛 version의 입력도 실패한다. 5초 동기화로 회원 입장 창이 열린 상태에서도 종료를 알려주고 화면을 잠근다.
- 시드/순위/정모/출석/승패 데이터와 무관하다. 운영 현장 세션을 테스트 목적으로 열거나 닫지 않는다.

## 13. 실시간대진 중복 이름 안내 (요청 097)

- 096과 함께 `4ddc566` 배포. 공개 코드 문구/확인 버튼 확인, 실제 거절/재입장/동시 입력 및 PC·모바일 화면은 로컬 가상 데이터로 검증했다.

- 현재 코트의 이름과 같으면 서버가 참가 요청을 거절한다. 빈자리가 남은 코트/4명이 찬 코트 모두 검사하며 앞뒤 공백을 제거한다. 다른 코트나 다음 대진 대기열로 옮겨 등록해도 우회할 수 없다.
- 정확한 문구 `현재 게임중인 회원입니다. 등록할 수 없습니다.`와 단일 `확인` 버튼의 안내창을 표시한다. 안내창을 닫아도 뒤의 참가 입력은 보존하여 이름을 고칠 수 있다.
- 대기열에만 이미 있는 이름도 기존처럼 차단하되 `이미 다음 대진에 등록된 이름입니다.`로 구분한다. 개별 코트 종료로 현재 명단에서 빠진 이름은 재참가 가능하다. 거절된 요청은 DB/버전을 바꾸지 않는다.
- 동시 같은 이름 등록은 version CAS로 한 건만 저장한다. 점수/출석과 무관하며 로컬 회귀 데이터로만 검사한다.

## 14. 전체 기능 안정성 점검 (요청 098, 2026-09-15)

- `0014_roster_write_revision.sql`은 변경 버전 행만 생성한다. 기존 회원/게스트 점수와 정모 데이터는 변경하지 않는다. `roster-write.js`의 공통 버전을 먼저 읽고 `commitRoster`가 첫 SQL에서 검사해 다른 대진의 동시 마감·회원 수정·이관·숨김과 충돌하면 전체 작업을 롤백하고 409를 반환한다. 새 명단/점수 쓰기도 이 경로를 사용해야 한다.
- 회원 정보·변경 이력·순위 재배치는 한 D1 batch로 저장한다. `updateRows`/`insertRows`는 신뢰된 내부 테이블·열 이름과 JSON 바인딩으로 일괄 처리한다. 200명·20라운드 마감 14쿼리/취소 16쿼리, 150명 숨김·35명 이관 각각 9쿼리를 로컬에서 확인했다.
- 마감/취소가 회원/게스트 `edit_version`도 올려 이전 수정창이 점수를 덮어쓰지 못한다. 명단 숨김도 이력을 남긴다. 같은 정모의 진행 변경/삭제/마감 충돌 보호는 기존 게시글 버전과 함께 유지한다.
- 마감과 취소는 화면에 표시된 게시글 버전으로 요청한다. 최신 GET 후 자동 마감하지 않는다. 충돌하면 최신 화면을 확인하고 다시 누른다. 같은 마감 operation의 네트워크 재전송은 한 번만 반영하며, 다른 operation의 오래된 확인은 409다. 취소 API의 버전 미전송은 이전 탭과의 호환을 위해 허용한다.
- 참가 ID 배열을 엄격히 검증하며, 명시한 미등록 ID를 이름이 같은 회원으로 대신 정산하지 않는다. 이관 후 이름이 변경된 게스트도 `promoted_guest_id`로 현재 회원에 연결한다. ID가 아예 없는 옛 대진만 이름 매칭을 유지한다.
- 백업은 7개 원본 테이블을 한 읽기 트랜잭션으로 가져와 정산 전후 데이터 혼합을 막는다. 실제 관리자 비밀번호 설정·과거 자동 백업 실행 이력은 로컬 시험만으로 확인한 것으로 간주하지 않는다.
- 로그인/사람 이력/수정/대진 선택창의 취소·이동 후 늦은 응답을 차단한다. 즉석 게스트는 해당 대진 선택창의 로컬 목록에만 둔다. 확인창의 뒤로가기·ESC·예/아니오 모두 Promise를 완료하고 리스너를 정리한다.
- 생성기는 소수 코트·라운드, 잘못된 방식/참가자/늦참 값을 거절한다. 기존 늦참/휴식/균형 우선순위·수기 다른 경기 중복 출전 규칙은 유지한다.
- `check-server-audit.mjs`, `check-client-audit.mjs`를 `check-login.mjs` 필수 검사에 통합했다. 서버 경쟁/실패/대규모 검사는 운영 DB가 아닌 메모리 SQLite에서 실행하며 D1 batch 전체 롤백을 모사한다. 실제 클라이언트 함수와 PC·390px 브라우저를 함께 확인한다.
- 공개 승패 기록과 자유대진 참가/취소/종료는 기존 신뢰 기반 권한 모델이다. 이번 안정성 수정으로 개인 계정 인증을 새로 도입하지 않는다. 배포·공개 검증 결과는 요청 098 상세 기록을 따른다.

## 15. 실시간대진 다중 등록·빈자리·대기 취소 (요청 099, 2026-09-15)

- 16:14 KST `ba7688a` 기능 및 `250f8c4` 설정 보완 공개 반영 확인. Cloudflare 활성 버전 `ca7d740f`, 빌드 성공·트래픽 100%. 기존 배포 실패 원인은 Cron 요일 `0`이었다. `wrangler.jsonc`의 `0 0 * * SUN`은 일요일 09:00 KST 자동 백업으로 유지한다.
- `join` 요청은 기존 단일 `name`과 새 `names` 배열을 모두 받는다. 새 화면은 공백을 제외한 1~4명을 한 번에 보내며, 같은 묶음 안 중복·현재 코트 중복·대기열 중복을 서버에서 원자적으로 거절한다.
- 일반 참가 시 선택한 현재 코트 또는 대기 그룹의 빈자리를 앞에서부터 채운다. 남은 빈자리보다 입력 인원이 많으면 저장하지 않고 가능한 자리 수를 안내한다.
- 대기 상태의 각 이름 옆 작은 원형 X는 기존 `leave`로 한 명만 비운다. 비워진 칸에는 작은 원형 `+`가 생기며, 이를 누르면 `group`과 `slot`을 포함한 단일 `join`으로 그 자리만 다시 채운다.
- 대기 중인 현재 코트와 대기열 각 그룹 오른쪽에 `대기 취소`가 표시된다. `대기 취소하시겠습니까?`에서 `예`를 누른 경우에만 새 `cancel` 동작이 해당 현재 코트 전체 또는 해당 대기 그룹 전체를 비운다. `아니오`는 저장 요청을 보내지 않는다.
- 모든 변경은 기존 live-courts version CAS를 그대로 사용한다. 충돌하면 최신 상태를 다시 불러오므로 다른 기기의 선행 입력을 덮어쓰지 않는다. 정모 대진·시드·출석·승패·정산 테이블에는 관여하지 않는다.
- 필수 회귀 `check-live-courts.mjs`와 실제 클라이언트 회귀 `check-live-ui.mjs`에 4명 일괄 등록, 원자적 중복 거절, 현재 코트/대기열 정확한 빈자리 재등록, 전체 대기 취소, 예/아니오 동작을 추가했다.

## 16. 동일·인접 팀 균형 및 최신 점수 (요청 100)

- `be7ef8a`, 2026-09-15 19:14 KST 공개 반영 확인. 공개 새 코드·주요 API 정상, 명단/점수/순위/기존 대진/사용 중인 자유대진 보존. 새로고침 후 새 생성/코트 추가부터 적용된다.

- 기본 생성기에 현재 숫자 DB 점수를 넣으면 예시 네 명은 이미 최적 조합이었다. 그러나 페이지 명단 캐시는 오래된 점수를 재사용했고, `＋ 코트 추가`는 팀 균형 계산을 우회하여 앞의 두 명을 그대로 팀으로 만들었다. 공개 추가 코트와 로컬 코드에서 같은 불균형을 확인했다.
- `getPeople(true)`를 대진 선택창 시작·생성 직전·시드현황·동일/인접 코트 추가에서 호출한다. 늦게 도착한 이전 조회가 최신 캐시를 덮지 못하게 순번을 둔다. 최신 읽기에 실패하면 오래된 점수로 생성하지 않고 재시도를 안내한다.
- `schedule-tools.js`의 `balanceFour(roster4, method, previousMatches=[])`는 `{id,name,points}` 객체 네 개를 받아 같은 형태의 팀 순서 `[A1,A2,B1,B2]`를 반환한다. 이전 경기들도 같은 객체 배열이다. 세 가능한 팀 분할 모두에서 합계 차이 최소→동률인 경우 파트너 반복 최소 순으로 선택한다. 생성기도 같은 내부 선택기를 사용하며 라운드별 파트너 횟수는 누적한다. 숫자 문자열도 숫자로 처리한다.
- `addBalancedCourt`는 기존 휴식자/참여자 우선 네 명 선택을 유지하고 새 코트만 균형 배치한다. 다른 경기나 수기 수정 자리는 바꾸지 않는다. 비동기 추가 중 재클릭/페이지 이동/저장/다른 편집은 오래된 응답으로 초안을 변경하지 못한다.
- 점수 차이를 줄이기 위해 다른 코트의 4인 그룹까지 재구성하지는 않는다. 동일 가까운 4명, 인접 1256/3478 묶음·랜덤·늦참·휴식·정산 규칙은 유지한다. DB 미등록 즉석 참가자의 실제 실력 점수는 추정하지 않는다.
- 실제 UI·생성기 회귀 및 로컬 브라우저로 주밤116+두진97(평균106.5) vs 아식스110+시오104(평균107)를 확인했다. 기존 공개 대진은 임의 재편성하지 않는다. 배포 결과는 요청 100 상세 기록을 따른다.

## 17. 실시간대진 사전 등록과 회원별 대기 시간 (요청 101)

- 배포 상태(2026-09-17 23:46 KST 후속 확인): 기능 aec9c6d 공개 반영 확인 완료. 공개 API participants/serverNow·등록 UI 코드 확인, live version 51. 01:18에는 이전 코드여서 대기였으나 이제 해당 대기는 해소됐다. 운영 데이터를 시험으로 변경하지 않았다.

- §15의 자유로운 이름 입력은 사전 등록 후 선택 방식으로 바꾼다. 실시간대진이 열린 동안 누구나 `참가 명단 등록`에서 이름 1~4개를 추가한다. 자유대진 전용 명단이며 시드 회원/게스트 DB와 연결하거나 점수를 변경하지 않는다.
- `live_courts` 현재 JSON의 `participants:[{name,waitingSince}]`에 고유 이름과 서버 ISO 시각을 저장한다. 추가 SQL 마이그레이션이 없다. 기존 코트/대기열에 있던 이름은 조회 시 자동 호환하고 다음 정상 CAS 저장에 영속화한다. 옛 시작 시각은 알 수 없으므로 기존 updated_at을 사용한다. 조회만으로 운영 DB를 쓰지 않는다.
- API `register`는 개방 세션만 허용하며 이름 1~40자·앞뒤 공백 제거·제어문자/중복 차단·한 번에 최대 4명·전체 최대 500명이다. 클라이언트가 보낸 대기 시각은 무시한다. `join`은 등록된 이름만 허용하고 기존 코트/다음 대진 중복 차단을 유지한다. 모든 변경은 version CAS다.
- `대기중인 회원`은 다음 대진 아래에 둔다. 아직 실제 경기에 들어가지 않은 등록자(부분 코트/대기열 배정자 포함)의 이름 아래 분:초를 표시한다. 먼저 기다린 순으로 보여주며 배정된 곳을 작게 안내한다. 배정된 회원은 다른 대진에 중복 선택할 수 없다.
- 기존 규칙대로 실제 코트 4명이 차면 자동 경기 중이다. 이 4명의 대기 시계는 화면에서 숨긴다. 코트 `end`로 나온 4명만 waitingSince를 종료 시각으로 갱신한다. FIFO 다음 그룹이 4명이면 바로 경기 중, 부족하면 대기 시간을 이어 간다. `leave/cancel`은 대기 시각을 초기화하지 않는다.
- 전체 `close/open`은 명단/시각을 유지한다. 닫힘 공개 GET은 courts/queue/participants 모두 숨기고 입력을 막는다. 코트 재생성은 기존 초기화 확인을 거치며 등록 명단은 유지하고, 없어진 진행 경기 참가자만 새 대기로 돌린다.
- `serverNow`를 응답에 포함해 화면의 기기 시각 차이를 보정한다. 저장된 waitingSince에서 경과시간을 계산하여 초당 숫자만 갱신한다. 초당 DB 쓰기 없음. 화면 종료 시 초 타이머/기존 5초 동기화/대화상자를 정리한다.
- 입력창에서 시작한 충돌 재조회는 요청 당시 창의 동일성을 응답 반영 전에 확인한다. 취소한 등록/참가 창의 늦은 조회가 새 빈자리 창의 버전을 올려 오래된 그룹 번호를 최신 버전으로 저장하게 해서는 안 된다. 일반 동기화는 열린 입력창에 대한 전체 종료 감지 외에는 입력 모델을 교체하지 않는다. 단일 `+` 입력이 충돌하면 창을 닫고 최신 빈자리를 다시 선택하도록 안내한다.
- 회귀와 배포 결과는 101 상세 기록을 따른다. 운영 현장에서는 테스트 명단 등록/개방/종료/정산을 수행하지 않는다.

## 18. 동일·인접 전체 대진 파트너 중복 최소화 (요청 102)

- 기능 커밋 `1df0f5b`, 2026-09-17 23:53 KST Cloudflare 빌드·GitHub CI 성공 및 공개 새 코드 확인. 23:56 기존 회원/점수/대진 해시 불변. 새로고침 후 새 자동 생성부터 적용한다.
- 사용자 '혼합'은 현재 UI 인접(`balanced`)으로 해석했다. 코트별 4명 안에서 해결되지 않으면 **같은 라운드의 다른 코트 선수와 교환**하는 것을 사용자가 승인했다.
- §16의 점수 차이 우선 규칙은 이번 생성 요청에서 바뀐다. 동일/인접 전체의 팀 파트너 추가 반복 횟수 최소 → 같은 쌍 반복 집중도 최소 → 팀 합계(평균) 차이 최소 순으로 비교한다. 랜덤은 집계·재배치에서 제외한다.
- `schedule-tools.js` 내부 `repairPartners`는 생성이 끝난 전체 비랜덤 경기에서 4명 내 분할, 두 고정 경기의 분할 조합, 같은 라운드 두 코트 8명의 315가지 분할을 순서대로 비교한다. 코트 교환은 파트너 반복이 개선될 때만 하고 점수만을 위해 실행하지 않는다. 늦참/휴식/라운드별 출전자·코트 수 불변.
- 탐색에는 후보 횟수 제한이 있으며 전역 최적해/중복 0을 보장하지 않는다. 다만 채택한 변경은 반복 횟수/집중도/점수 차이 목적함수를 악화시키지 않는다. 무한 재시도하거나 실패 시 빈 대진을 돌려주지 않는다.
- `partnerSummary(schedule)`는 랜덤 제외 `{duplicateTeams,pairs:[{names:[a,b],count}]}`를 반환한다. `duplicateTeams`는 각 파트너 쌍의 첫 등장 이후 추가 등장 횟수 합계다. 편집 화면에서 없거나 남은 중복을 안내하며 수기 변경 시도 현재 값을 보여준다.
- `balanceFour`에 넘길 이력은 호출자가 비랜덤만 필터한다. 추가 코트는 이전/이후 라운드를 포함한 대진 전체를 참조하고 **새 코트만** 구성한다. 이미 저장된 경기나 수기/승패 기록을 자동 재편성하지 않는다. 전체 재탐색은 새 자동 생성 또는 사용자가 참가자·코트 변경에서 다시 만들기를 선택할 때 적용된다.
- 필수 검사와 Astra 추가 검토, 로컬 실제 8명·7라운드·28팀 파트너 고유·저장 확인 PASS. 배포 상태는 102 기록과 메인.md를 따른다.

## 19. 동일 경기 점수순 묶음·세 조합 순환 (요청 103)

- 기능 커밋 `85803ba`, 2026-09-18 00:27 KST 공개 반영 확인. Cloudflare/GitHub CI 성공·공개 순환 안내/검증 코드 확인·기존 회원/점수/대진 해시 불변. 새로고침 후 새 생성/추가 코트부터 적용.
- §18 중 동일 경기의 중복 우선 팀 변경/코트 교환은 이번 사용자 지정 규칙으로 대체한다. 각 라운드의 기존 휴식/늦참 제외 후 출전자(4의 배수)를 숫자 점수 내림차순으로 정렬해 가까운 4명씩 묶는다. 동일 점수는 입력 명단 순서를 유지한다.
- 전체 대진 중 동일 라운드의 순서만 센다. 첫 번째 `1·4 vs 2·3`, 두 번째 `1·3 vs 2·4`, 세 번째 `1·2 vs 3·4`, 네 번째부터 반복한다. 랜덤/인접은 동일 회차를 올리지 않는다. 참가 가능한 사람이 없어 빈 동일 라운드라도 설정된 동일 순서에는 포함한다.
- 늦참/휴식으로 출전자가 바뀌면 그 라운드 출전자 점수로 묶음을 다시 만든다. 고정이라는 의미는 생성 후 최적화가 해당 묶음/팀을 변경하지 않는다는 뜻이지, 모든 라운드의 4인 명단이 같다는 뜻이 아니다.
- `sameFour`는 점수 정렬과 지정 조합을 수행한다. `balanceFour(roster4, method, previousMatches=[], sameIndex=0)`의 sameIndex는 현재 라운드 앞에 있는 동일 라운드 수(0 기반)다. 추가 코트도 같은 값을 전달하여 새 코트만 배치한다. 기존 수기 수정/승패/다른 코트는 건드리지 않는다.
- `repairPartners`는 동일·인접 모두 파트너 횟수를 집계하되, 변경 가능한 entries에는 인접만 넣는다. 인접이 한 경기만 있어도 고정 동일 이력과 비교한다. 동일이 지정 조합 때문에 불균형/반복이어도 이를 다시 바꾸지 않는다. 인접은 반복 최소·동률 균형과 허용된 인접 코트 간 교환을 유지한다.
- 검증/배포 상태는 메인.md와 요청 103 상세 기록을 따른다. 운영 DB나 이미 저장된 대진을 재편성하지 않는다.
