# 코덱스 배포 지시문 (복사해서 코덱스 대화에 붙여넣기)

> **2026-09-12 02:20 이후 사용하지 않음.** 요청 025로 호스팅을 GitHub + Cloudflare 구조로 옮기기로 해서, 옛 Codex Sites에 배포하는 이 지시문은 보류한다. 새 절차는 `kokkiri-site\README.md`와 `메인.md` '현재 상태' 참고. (옛 사이트를 굳이 최신으로 올려야 할 때만 이 문구를 쓴다.)

- 작성: 2026-09-12 01:58 KST, Claude Code
- 붙여넣을 곳: 코덱스의 배드민턴 스레드("배드민턴 점수와 순위의 변동을 자동적으로 확인할 수 있는 프로그램을 만들고 싶어")
- 배포는 코덱스 전용 도구(소스 저장소 쓰기 자격 발급, 버전 저장, 배포 실행)로만 가능해서 Claude Code에서는 실행할 수 없다. 아래 문구를 그대로 보내면 된다.

---

계속 진행해줘. 콕끼리 홈페이지를 공개 배포해줘.

상황:
- 홈페이지 원본 폴더 `C:\Users\just\Desktop\배드민턴 프로그램 만들기\kokkiri-site` 의 main 브랜치가 origin/main보다 6커밋 앞서 있어. 작업 트리는 깨끗해. 소스는 더 고치지 말고 그대로 배포해줘.
- 포함 내용: 회원 화면 운영진권한 버튼(비밀번호 로그인), 공지사항 날짜·수정됨 표시를 운영진 화면에서만 표시, 홈 카드 화살표 삭제·가운데 정렬, 하단 "콕끼리 Since 2026.05.08. 우리가 함께한지 N일", 출석·승패 점수 누적과 시드 자동 반영(D1 마이그레이션 0002·0003 포함), 주밤·로토 명단 시드를 점수 규칙대로 A로 정정.
- 로컬 검증: `check-login.mjs` 통과 확인됨(2026-09-12 01:53).
- 비밀 환경변수 OPERATOR_PASSWORD 는 지난 세션에 이미 설정했다고 했으니 설정이 살아 있는지만 확인해줘. 값을 다시 물어볼 필요는 없어.
- 공개 범위는 지금 사이트와 같게(링크를 가진 누구나 열람) 유지해줘.

순서: 원본 업로드(push) → 새 버전 저장·배포 → 공개 주소 https://kokkiri-badminton-draw.rkdtjdwns1312.chatgpt.site 에서 아래 확인 → `C:\Users\just\Desktop\콕끼리 홈페이지 관리\메인.md` 의 '현재 상태와 미완료 사항'을 배포 완료로 갱신하고 `기록` 폴더에 기록 파일을 남겨줘(규칙은 그 폴더의 메인.md와 코드 폴더의 AGENTS.md 참고).

배포 후 확인할 것:
1. 회원 홈 오른쪽 아래에 '운영진권한' 버튼이 보이고, 비밀번호를 넣으면 운영진 화면으로 넘어간다.
2. 회원 화면 공지사항에는 날짜와 '수정됨'이 안 보이고, 운영진 화면에는 보인다.
3. 홈의 공지사항·대진표·시드현황 카드에 화살표가 없고 가운데 정렬이다.
4. 맨 아래에 "콕끼리 Since 2026.05.08. 우리가 함께한지 N일"이 있고 숫자만 초록색이다.
5. 시드현황에 회원 랭킹 표(순위·변동·시드·점수·출석·승·패)가 나오고 주밤·로토가 A로 표시된다.
6. 운영진 대진표 편집에 코트별 승패 버튼과 '점수 반영 및 확정' 버튼이 있다. 실제 운영 대진표로 테스트 정산은 하지 마.

---

## 배포가 끝난 뒤 사용자가 직접 확인할 것

- 위 1~6번을 휴대폰이나 PC 브라우저에서 한 번 눌러 본다.
- 비밀번호 창에서 Enter 키로도 제출되는지 확인한다(버튼 클릭은 로컬에서 확인됨).
