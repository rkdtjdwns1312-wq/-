# 118 — 실시간대진 열기와 코트 수 선택 통합

## 요청

- 실시간대진의 별도 코트 생성하기 버튼을 없앤다. 운영진이 실시간대진 열기를 누르면 코트 개수를 몇 개로 할까요?라는 안내와 1~9의 스크롤 선택 목록을 보여준다.
- 기존 시작/종료 예·아니오 확인, 운영진만 개방, 회원 참가/대기열/경기 종료, 시드 점수 미반영 규칙은 유지한다.

## 계획과 분담

- 시작 HEAD 881e451, clean. git pull --ff-only origin main 최신 확인.
- 본체: 선택한 코트 생성과 개방을 단일 버전 조건부 DB 쓰기로 묶고 서버 회귀 검사·문서·통합/공개 검증.
- Luna: 좁은 범위 UI와 실제 UI 검사. Astra: 최종 검토.
- 운영 데이터에 시험 개방·종료·참가·정산을 실행하지 않는다. 기존 열린 게임과 명단은 배포로 변경하지 않는다.

## 현재 상태

- 공개 반영 완료(22:53 KST). 새 UI는 open 요청에 count 1~9를 보내고 취소는 쓰지 않는다. 예전 열린 페이지와 기존 API 호출의 호환성을 보존하되 진행 중 세션을 재열기로 초기화하지 않는다.
- 변경: dist/server/live-client.js, live-courts.js, check-live-ui.mjs, check-live-courts.mjs, 메인.md, 프로젝트_인수인계.md, 이 기록. DB 마이그레이션 없음.

## 검증

- 필수 node check-login.mjs 전체 PASS. count 범위·정수 검사, 회원 개방 거절, 동시 개방 한 번만 성공, stale 409, 이미 열린 게임 재개방 시 명단 보존, 점수/정모/원장 무변경.
- 실제 직렬화 UI 검사: 생성 버튼 없음, 정확히 1~9 옵션, 기본 1, 9개 선택 POST, 취소/Escape 무저장, 실패 후 재시도와 오래된 버전 보호. Luna 작업 및 단독 검사 PASS.
- Astra 최종 지정 4개 코드/검사 파일 read-only 검토 PASS. 구체적 버그 없음, CAS·호환·취소·중복 클릭·stale 검사 확인.
- 로컬 메모리 DB 브라우저: 9 선택 후 아니오 → 닫힌 상태 유지, 다시 9 선택 후 예 → 1~9번 코트와 빈 36칸 생성. 종료 아니오 → 9코트 유지, 종료 예 → 모두 사라짐. 종료창에는 개수 선택 없음.
- 320px: 선택창·문구·예/아니오 잘림 없음, 선택 요소 폭 229px. 기존 정상 종료 기능 유지. 시험 데이터는 로컬 메모리 DB에만 생성.
- 공개 착수 기준: live version 166, isOpen false, 코트/대기열/등록 명단 0. 공개 API 5종을 읽기만 해 SHA-256 비교 기준으로 보관(serverNow 제외).

| 기준 API | SHA-256 |
|---|---|
| schedule | 3bf962c7675f180e1c85f8654119ee775eab45826b3f0ca344091a233535aa6d |
| notice | d7010323d9b2f3501b105e9b831d4b5843dfc3ed8d50a553240558de7fab5c7b |
| rankings | 27e73fd4ff19a3d92679c851cd78cd2be54ffe70b9ca0854a448f57546402ca2 |
| people | 83361a045b20098bf1ac37ef4361a31e7eb7a373ea5f260c59fff8f3ff5ea90a |
| live | 61060550e62c866a762a886c04992b1f7dcb9cc208944cbd3582a09fa961dbeb |

## 핵심 명령

- 번들 git pull --ff-only origin main: 최신.
- node check-login.mjs PASS, 로컬 UI/Astra PASS.
- 기능 커밋 96b2b88c31ebe7230ed265aa8a692322e1acf061 → git push origin main. GitHub test success(22:51:25), Cloudflare Workers Builds success(22:51:38 KST).
- 공개 운영진 화면에서 생성 버튼 0개, 열기 선택창 정확히 1~9/기본1 확인. 9 선택 후 아니오로 닫힌 상태 유지. 실제 공개 코트 개방/종료/참가/정산은 실행하지 않았다.
- 배포 후 위 5개 API의 SHA-256이 착수 기준과 전부 일치. live v166/닫힘/코트0/대기0/참가0 유지. 데이터·시드 원장 변경 없음.
- 작업용 브라우저 탭·메모리 DB 서버·Luna/Astra 에이전트 정리, 임시 viewport 초기화. 본 문서 포함 최종 기록도 검사 후 commit·push한다. 미완료 기능 없음.
