# 왕관을 시드현황표에만 표시

- 작업 시각: 2026-09-12 13:22 ~ 13:26 KST
- 작업 도구: Claude Code (Fable 본체 직접 처리 — 소규모 수정, 테스트·로컬 DOM 확인)
- 관련 요청: 044

## 1. 요청 번호와 요청 요약

- 요청 044: 대진에서는 왕관 표시가 없어도 된다. 다만 시드현황에서는 운영진임을 알 수 있게 시드현황표에서만 왕관이 있으면 된다.

## 2. 계획 (Fable/본체)

- `crown()` 호출 3곳 중 시드현황 회원 표만 남기고, 대진 상세(scheduleHTML player)와 대진 만들기 참가자 선택(openPicker render)에서 제거. 운영진 판별 로직(generate의 `OPERATORS.has`로 쉬는 순서 결정)은 시각 표시와 무관하므로 그대로 둔다.

## 3. 수행 내용과 변경 파일 (Fable 직접 처리)

- `dist/server/boards-client.js`: scheduleHTML view player를 `esc(m[si])+crown(m[si])`→`esc(m[si])`. openPicker render의 `esc(p.name)+crown(p.name)`→`esc(p.name)`. seeds 회원 표(`esc(row.name)+crown(row.name)`)는 유지. `crown()` 헬퍼·OPERATORS 집합은 존치(시드표·역할 순서에서 사용).

## 4. 검증 결과 (본체 검토 + 테스트 + DOM)

- `node check-login.mjs` PASS.
- `crown(` 호출 잔여: 시드현황 회원 표 1곳뿐.
- 로컬 DOM: 대진 상세 `.match .crown` 0개(운영진 이름도 왕관 없음), 시드현황 `.ranking-table .crown` 6개(로토·이코·뉴키·구구·단우·백구).

## 5. 최종 점검 (Fable/본체)

- 요건 충족: 대진·참가자 선택 왕관 제거, 시드현황표에만 왕관 유지. 042의 `.player .crown` CSS는 이제 매칭 대상이 없어 무효(무해)하여 존치.

## 6. 미완료 사항과 다음 단계

- 없음.

## 7. Git 동기화·배포 결과

- 커밋 후 GitHub push → Cloudflare 자동 배포.
