# 홈 메뉴 초록 강조를 호버 시에만

- 작업 시각: 2026-09-12 13:12 ~ 13:16 KST
- 작업 도구: Claude Code (Fable 본체 직접 처리 — 소규모 CSS/HTML 버그 수정, 테스트·로컬 화면 확인)
- 관련 요청: 043

## 1. 요청 번호와 요청 요약

- 요청 043: 첫 화면부터 대진표 메뉴가 선택된 것처럼 초록으로 표시되는 버그 수정. 초록 강조는 마우스 커서가 올라갔을 때만, 3개 메뉴 각각에.

## 2. 원인과 계획 (Fable/본체)

- 원인: 대진표 카드에 `class="menu featured"`가 붙어 있고 `.menu.featured{background:#eafff1;border:2px solid #03b958}`가 항상 초록으로 강조. `.menu:hover`에는 초록이 없어 lift/shadow만 있었다.
- 계획: 대진표의 `featured` 클래스 제거(3개 카드 동일화), `.menu.featured` 규칙 제거, 초록 강조를 `.menu:hover`로 이동(모든 메뉴 공통, 호버 시에만).

## 3. 수행 내용과 변경 파일 (Fable 직접 처리)

- `dist/server/boards-client.js`: 홈 대진표 카드 `class="menu featured"`→`class="menu"`.
- `dist/server/boards-style.js`: `.menu.featured{...}` 제거, `.menu:hover`에 `background:#eafff1;border-color:#03b958` 추가(기존 transform·shadow 유지, border는 색만 바꿔 두께 변화로 인한 밀림 없음).

## 4. 검증 결과 (본체 검토 + 테스트 + 화면)

- `node check-login.mjs` PASS. 소스에 `featured` 잔여 참조 0건.
- 로컬 화면: 홈 기본 상태에서 공지사항·대진표·시드현황 3개 모두 흰색(초록 강조 없음). 대진표에 마우스 호버 → 해당 카드만 연초록 배경+초록 테두리+살짝 뜸, 나머지는 흰색.

## 5. 최종 점검 (Fable/본체)

- 버그 해소: 상시 초록 강조 제거, 호버 시에만 3개 메뉴 각각 초록. 다른 기능 영향 없음.

## 6. 미완료 사항과 다음 단계

- 없음.

## 7. Git 동기화·배포 결과

- 커밋 후 GitHub push → Cloudflare 자동 배포.
