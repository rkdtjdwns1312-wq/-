# 119 — 회원 로그인 암호 정정

## 요청

- 사용자 정정에 따라 회원 공통 로그인 암호를 새로 지정한 값으로 변경한다. 실제 새/옛 암호 및 운영진 자격은 이 문서·소스·명령 파일에 기록하지 않는다.

## 진행

- 시작 HEAD 12228ed, clean. 최신 pull --ff-only 결과 이미 최신.
- 기존 운영진 전용 member-access/config API는 현재 설정 version을 받아 원자적으로 변경하며 원문 대신 salt·PBKDF2/HMAC 검증값만 DB에 저장한다.
- 코드 변경이나 새 배포는 불필요. 본체 직접 처리, 별도 구현 위임 없음. 운영진 암호는 변경하지 않는다. 기존 회원 쿠키는 설정 version 변경으로 만료된다.
- 01:44 KST 운영진 인증 HTTPS API로 설정 version 1→2 교체 완료. 실행 프로세스 메모리에서만 새 암호를 전달했다.
- 공개 검증: 변경 전 회원 입장 200, 변경 후 이전 회원 세션 보호 조회 401, 옛 암호 로그인 401, 새 암호 로그인 200 및 보호 메뉴 조회 200. 운영진 설정 조회는 계속 정상.
- 변경 전후 schedule/notice/rankings/people/live(serverNow 제외) 5종 SHA-256 전부 동일. 운영 명단·점수·대진 수정 없음. 본체 직접 확인, 코드 변경이 없어 구현/검토 하위 에이전트는 만들지 않았다.

## 핵심 명령

- git pull --ff-only origin main: 최신.
- 설정 변경 및 실제 인증 검증 완료. 문서 동기화 전 필수 node check-login.mjs 검사 → 문서 commit → git push origin main. 암호 반영은 DB 설정 변경 즉시 완료되며 문서 배포 대기와 무관하다.
