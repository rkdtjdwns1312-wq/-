# 103 — 동일 대진 점수순 4명 묶음과 세 조합 순환

- 착수: 2026-09-18 00:15 KST, main f97d6e5 clean, pull --ff-only 최신 확인.
- 사용자 요청: 휴식을 제외한 출전자를 시드점수 순으로 세워 4명씩 묶고, 그룹 내 1~4위의 팀을 14:23 / 13:24 / 세 번째 동일 경기에는 12:34로 구성.
- 적용 해석: 전체 대진의 동일 라운드 순번만 센다(랜덤/인접 제외). 4회부터 세 조합 순환. 매 라운드 늦참·휴식을 먼저 처리하고 남은 출전자 전체를 점수 내림차순으로 정렬한다. 동점은 안정적인 순서로 처리한다.
- 요청 102 변경: 동일 경기의 지정 조합과 코트 4인 묶음을 중복 탐색으로 바꾸지 않는다. 인접은 동일의 고정 파트너도 집계하면서 자신만 중복 최소화 재배치한다. 랜덤은 집계/재배치 제외.
- 수기 수정/기존 저장 대진/점수/승패/실시간대진 DB는 자동 변경하지 않는다. 새 생성 및 동일 추가 코트에 적용한다.

## 분담 및 검증 계획

- 본체: 생성기·동일 조합 잠금·회귀·통합·기록.
- Terra: 추가 코트의 동일 순번 전달 및 안내 문구·클라이언트 회귀(boards-client.js/check-client-audit.mjs만).
- 마지막 Astra 검토 및 필수 check-login.mjs PASS 후 commit/push, 공개 새 코드 읽기 확인.
- 운영 데이터 시험 쓰기는 하지 않는다.

## 구현 및 검증 결과

- sameFour: 점수 숫자 정렬·입력순 동점 유지·sameIndex%3 지정 조합. generate는 휴식/늦참 처리 후 가까운 4명을 만들고 동일 라운드만 순번을 올린다. 빈 동일 라운드도 설정 순서에 포함한다.
- repairPartners는 동일 파트너를 고정 이력에 넣고 인접만 변경한다. 인접이 1경기뿐이어도 이후 동일까지 참조해 중복을 줄인다. 랜덤 제외 및 기존 휴식/늦참 보존.
- 추가 코트는 이전 동일 라운드 개수만 전달하여 해당 회차에 맞는 새 팀을 만든다. 현재/미래 코트 수나 랜덤/인접은 순번에 포함하지 않는다.
- 00:20 전체 check-login.mjs PASS. 160개 경계 사례·늦참/휴식 역할별 사례·혼합 방법 순서·동점/문자열 점수·입력 명단 불변·200명 20라운드의 동일 묶음/조합 검증 포함.
- 기존 요청 102의 '동일 8명 7R 전원 서로 다른 파트너' 테스트는 이번 고정 묶음 규칙과 충돌하므로 인접 사례로 전환해 코트 간 탐색 검증을 유지했다. 동일 8명 8R는 정해진 반복 20건을 정확히 표시하며 코트 교환으로 숨기지 않는다.
- 병렬 UI 편집 중 1회 필수 검사가 이전 도움말 정규식에서 실패했다. Terra가 설명/회귀를 함께 갱신한 후 전체 PASS했다.
- Astra 읽기 전용 최종 검토: 수정 필요 결함 없음. 정확한 회차/정렬/동점·휴식/늦참·동일 잠금·인접 1경기·직렬화·수기/기존 승패 보존 검토 및 diff --check PASS. 필수 전체 실행 결과는 본체의 결과를 참고했다.
- 00:21~00:22 실제 로컬 UI에서 8명·2코트·동일 4R 생성. 두 코트 모두 14:23→13:24→12:34→14:23 확인, 중복 4건 표시, 대진 저장 완료. 로컬 DB 재조회 version 1·4라운드·8경기 정확한 조합·승패 빈 값 확인. 공개 DB에는 게시하지 않았다.
- 00:19 배포 전 공개 읽기 기준: rankings `311c4ae2304a81a0bd6f06d2af8a32689a63ebad8d558f26b71467d385d95782`, people `9e1b93757b29c4e8adaf160843eff4e2266eabf1ea7e7645d015ee0e6a8b7e76`, schedule 목록 `a1e82f6b7f3c9226886d81ac566265d9b7a6aef10b607be056ac8882a7518fde`, 모두 HTTP 200.

## 변경 파일 및 배포

- dist/server/schedule-tools.js, dist/server/boards-client.js, check-schedule.mjs, check-client-audit.mjs.
- 메인.md, 프로젝트_인수인계.md, 본 기록. DB/마이그레이션 변경 없음.
- 구현·로컬 검증 완료. commit/push 및 공개 읽기 확인 대기.

## 핵심 명령

- 번들 Git git pull --ff-only origin main
- 예정: 번들 Node node check-login.mjs → 코드/MD commit → git push origin main → 공개 읽기 확인
