# 107~115 — 실시간대진 개선·긴급교체·목록과 운영진 탐색 메뉴

## 요청과 시작 상태

- 107: GitHub 최신 작업 확인·오늘 버그 수정 준비. pull --ff-only origin main 결과 최신, HEAD/origin main d1cef4c 일치, 로컬 수정 없음. 직전 104~106 공개 완료 기록 확인.
- 108: 운영진이 실시간대진 종료를 누르면 코트가 모두 사라지고 대진 등록 회원도 모두 사라지게 변경.
- 범위는 live_courts의 courts/queue/participants 및 waitingSince. 회원/게스트 시드 명단·점수·정모 게시글/승패/정산에는 영향 없음. 기존 096·101의 전체 종료 시 보존 규칙을 대체한다.

## 구현 계획과 분담

- Codex 본체: 전체 종료 CAS 저장에서 코트·대기열·등록 명단 모두 초기화, 서버 회귀, 통합 검증/문서/배포.
- GPT-5.6 Luna(medium): 종료 확인창 초기화 설명·종료 후 UI·UI 회귀 (live-client.js/check-live-ui.mjs).
- 최종 GPT-6 Astra 검토, 필수 check-login.mjs PASS 후 commit/push. 각 코트 개별 대진 종료(end)의 FIFO·대기 복귀 동작은 유지.
- 종료 확인의 아니오/취소/권한 없음/오래된 버전은 기존 상태 유지. 종료 후 다시 열어도 이전 명단을 되살리지 않는다. 읽기 조회나 배포만으로 진행 중 운영 세션을 종료하지 않는다.

## 검증/배포 상태

- 진행 중. 운영 데이터 시험 변경 없이 로컬 메모리 DB에서 검증한다.
- 본체 서버 변경: close는 courts/queue/participants를 같은 version CAS에서 비운다. 이미 닫힌 옛 보존 데이터도 명시적 close에서 정리하고 완전히 빈 닫힘은 멱등 응답. 조회나 open 자체로는 기존 데이터를 삭제하지 않는다.
- 서버 회귀: DB payload 실제 빈 상태, 재개방 미복원/재등록, 권한/오래된 버전 거절, 종료와 참가/등록 경쟁, 레거시 읽기 보존 및 명시 종료 정리, 점수/게시글 불변 검사 PASS. 전체 check-login.mjs 1차 PASS.

## 후속 사용자 요청 109·110

- 109: 참가 창의 이름 드롭다운 대신 위쪽 빈 4칸, 아래 선택 가능한 등록자 이름을 아이콘형 버튼으로 표시하여 클릭으로 추가. 선택 취소와 최대 4명, 기존 1~4명 저장·목적 코트/대기열 선택은 유지한다.
- 110: 코트의 + 빈자리 추가 버튼 제거. 회원 추가는 참가 창으로 통합하고 기존 이름 취소(X)·대기 대진 취소·개별 코트 종료 동작은 유지한다.
- Luna의 108 UI 작업 이후 담당 파일을 넘겨 새 참가 UI/회귀를 보완할 예정이다. 최종 Astra 통합 검토, 원격 운영 데이터 시험 변경 없음.

- 추가 111: 저장된 미마감 정모 대진에서 운영진이 라운드·코트, 기존 멤버/교체 멤버를 수기로 지정하여 한 자리만 변경. 기존 승패·진행 상태·다른 대진은 유지하고 교체자는 해당 팀의 기존 결과를 승계한다. 기존 참가자의 출석 명단은 보존한다. 시드 회원/게스트의 고유 ID를 사용하며 미등록/동명이인/불참/해당 라운드 늦참/같은 경기 중복은 거절한다.
- 분담 추가: Terra 1(Turing)은 109·110 참가 UI와 회귀, Terra 2(Socrates)는 111 긴급교체 UI/회귀, 본체는 긴급교체 서버·정산/경합 회귀, Astra(Chandrasekhar)는 전체 최종 검토. Luna 108 종료 UI 완료·UI 회귀 PASS. Astra 108 서버 1차 추가 지적 없음.
- 배포 전 공개 조회(18:29 KST): rankings SHA-256 `27e73fd4ff19a3d92679c851cd78cd2be54ffe70b9ca0854a448f57546402ca2`, people `83361a045b20098bf1ac37ef4361a31e7eb7a373ea5f260c59fff8f3ff5ea90a`, schedule `1e914284837f65db12e8b0b71dccdcc39f4eb9a0047ea9d658a9ed1d0299a15f`. live(serverNow 제외) `bf6d9456a001aba1d262272c9baff0524bcd8a36369f51813586d634034ad556`, version165/open/4코트/등록28명. 현재 운영 중이므로 테스트 종료·입력 금지.

## 후속 요청 112~114와 통합 검증

- 112·114: 공지사항과 대진표 목록 항목 오른쪽 장식 화살표 모두 제거. 항목 링크·제목·날짜/MVP 등 내용은 유지한다.
- 113: 홈 복귀 링크를 얇은 초록 테두리·둥근 직사각형의 `홈으로 가기`로 변경. 처음에는 오른쪽 정렬 요청이었으나 최신 정정에 따라 **기존 왼쪽 정렬 유지**. 상세의 대진표/공지사항 목록 복귀 링크는 별도 유지.
- GPT-5.6 Luna(medium, Aristotle)가 112~114의 작은 UI 변경과 실제 목록 회귀를 담당한다. 앞선 Luna/Terra 작업과 긴급교체 코드는 보존한다. Astra 전체 최종 검토는 계속 진행 중.
- `check-login.mjs`에 `runSubstitutionChecks`와 `runEmergencyUIChecks`를 연결. 전체 회귀 PASS: 교체 회원/게스트 승·패·출석, 기존 참여자 출석 1회, 마감 취소 정확 원복, 레거시 ID, 권한/잠금/버전/명단 수정/마감 경합.
- 로컬 메모리 DB 브라우저 검증(18:44~18:46 KST): 선택창 4칸, 4명 초과 거절, 선택 취소·재선택·완료 후 코트 표시, 빈 코트에 + 없음, 전체 종료 아니오 보존/예 시 코트·등록자 제거. 390px 모바일 화면 확인.
- 로컬 대진에서 1R 1코트 승리를 기록한 뒤 네오→오웬 수기 긴급교체. 교체한 자리와 참가자만 추가되고 기존 승리/대진종료 표시는 유지, 1R 2코트의 네오 및 나머지 대진은 그대로임을 실제 화면 확인. 긴급교체 모바일 입력창 레이아웃 확인.
- 로컬 테스트 명단 초기화는 메모리 fixture에만 수행했다. 공개 운영 세션은 조회만 했고 종료/교체/정산하지 않았다.
- 공개 배포는 아직 확인 전이며 완료 여부·커밋은 아래에 후속 기록한다.
- Astra 검토에서 이관 전 게스트 ID와 현재 회원 ID가 서로 다른 이름으로 같은 게시글에 있는 경우 동일인 중복 배치 위험(P2)을 발견했다. 본체는 모든 참가자 별칭을 실제 회원 ID로 비교하도록 보완했고, 같은 경기 중복/불참/늦참 우회 모두 거절하는 회귀 PASS. 일반 마감 데이터 자체는 임의 수정하지 않는다.
- 115 추가: 운영진 모든 화면에 왼쪽 고정 세로 메뉴(홈/공지사항/대진표/실시간대진/시드현황), 초록 바탕·흰 글자. 별도 Terra(high)가 HTML/CSS 모듈과 회귀를 담당하고 본체가 서버 렌더링에 통합한다. 기존 해시 이동/미저장 편집 경고를 재사용하며 회원 화면에는 표시하지 않는다. 모바일 본문 겹침을 방지하도록 여백을 확보한다.

## 최종 검증

- 최종 로컬 검증(18:58 KST): 전 모듈 연결 check-login.mjs PASS, git diff --check PASS. 115 고정 메뉴 5곳 이동·공개 미노출·좌측 유지 확인. 320px에서 보완된 읽기/편집 카드 이름·입력칸 잘림 없음, 스크롤 중 고정 메뉴 위치 유지. 기존 .edit-match grid-area를 모바일 운영진 범위에서 auto로 재설정하여 암묵 3열 문제 해결. 새 테스트에 포함했다.
- 모델 분담 실제 결과: Luna 종료 UI, Terra 2개 참가/긴급교체 UI, Luna 목록·홈 버튼, Terra 고정 메뉴 모듈. 본체 서버·ID 별칭 보완·모바일 최종 수정·실행/브라우저 통합·문서/배포, Astra 최종 재검토. 구현자의 완료 주장과 별개로 본체가 실제 화면·필수 검사를 확인했다.
- GPT-6 Astra 최종 검토 PASS: 108~115 미해결 발견 사항 없음, 별칭 P2 수정·모바일 편집 grid 보완 확인. 필수 전체 검사·diff 검사 PASS를 독립 재확인했고 commit/push 단계로 진행한다.

## 공개 배포 결과 — 2026-09-19 19:02 KST

- 기능 커밋 `f758d17c8733b51ac2b9ad74126d07e1d7c853fb` main push 완료. GitHub test와 `Workers Builds: kokkiri` 모두 completed/success.
- 공개 홈페이지 HTTP200, 새 `홈으로 가기`, 긴급교체, 4칸 선택창, 운영진 sidebar CSS 반영 확인. 실제 공개 공지·대진 목록에서 화살표 제거·홈 버튼 확인, 회원 화면에는 운영진 메뉴 없음.
- 배포 후 `/api/rankings`, `/api/people`, `/api/posts?kind=schedule`, `/api/live-courts` 모두200. 위 18:29 KST의 SHA-256과 모두 동일(serverNow만 제외). 열린 실시간 세션 v165·4코트·등록28명 보존. 운영 데이터에 시험 종료·등록·교체·정산 없음.
- 새 기능은 새로고침하면 사용 가능. 전체 실시간 종료는 사용자가 확인 버튼을 누를 때만 초기화한다. 긴급교체는 시드현황에 등록된 이름을 정확히 입력해야 하며 이미 마감한 대진은 잠금 유지.
- 작업용 미리보기 서버 종료, 임시 브라우저/뷰포트·하위 에이전트 정리. 최종 배포 확인 문서도 필수 검사 후 별도 commit/push한다.

## 핵심 명령

- 번들 Git git pull --ff-only origin main: Already up to date
- 번들 Node node check-login.mjs PASS → 코드·문서 commit f758d17 → git push origin main → CI/Cloudflare success·공개 읽기 검증 완료
