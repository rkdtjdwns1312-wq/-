# Cloudflare 배포 완료와 하위도메인 변경

- 작업 시각: 2026-09-12 09:30 ~ 09:55 KST (사용량 한도 해제 후 재개)
- 작업 도구: Claude Code (Fable 직접 처리 — 브라우저 대행 작업, 설정 1줄 수정, 문서 갱신. 사용자 크롬(Claude in Chrome 확장)으로 Cloudflare·GitHub 화면 조작)
- 관련 요청: 028

## 1. 요청 번호와 요청 요약

- 요청 028 이어서: 사용자가 "모든 전권을 위임했고 다 동의한다"고 밝힘. 계정 설정(GitHub 앱 설치, D1, 저장소 연결, 배포, 하위도메인)을 에이전트가 대행하고 비밀값 입력만 사용자가 한다.

## 2. 계획 (Fable)

- 중단 직전 상태 확인(D1 ID 커밋 18d94be가 GitHub에 push되어 있음) → Cloudflare에서 GitHub 저장소 연결 → Worker 생성·첫 배포 → 새 주소 동작 확인 → 하위도메인을 모임 이름으로 변경(요청 026) → 비밀값 입력 화면까지 열어 사용자에게 인계 → 문서 갱신.

## 3. 수행 내용과 변경 파일 (Fable 직접 처리)

- GitHub 앱 연결: Cloudflare의 "Connect GitHub"는 팝업으로 GitHub 앱 설치 페이지를 여는데, 처음엔 별도 탭에서 설치해 Cloudflare가 결과를 받지 못했다(GitHub 본인확인 이메일 코드는 사용자가 입력). 앱을 제거한 뒤, 팝업이 같은 창의 탭으로 열리게 해서 다시 설치(저장소 `-` 하나만 선택, 권한 승인은 사용자가 사전 동의) → Cloudflare가 저장소 목록을 인식.
- Worker 생성: 저장소 `rkdtjdwns1312-wq/-`, 프로젝트 이름 `kokkiri`, Build command 없음, Deploy command `npx wrangler d1 migrations apply DB --remote && npx wrangler deploy`, 루트 `/`. 첫 빌드 #8ae708a4 성공(09:45): 마이그레이션 0000~0004 적용, 정적 이미지 2개 업로드, Worker 업로드(69KB), D1 바인딩 `env.DB (kokkiri)`.
- 첫 배포 오류와 수정: 홈 화면이 비어 보이고 콘솔에 `ReferenceError: __name is not defined`. 원인은 wrangler(esbuild) 기본 `keep_names`가 함수 안에 `__name(...)` 보조 호출을 넣는데, 서버가 회원 화면 스크립트를 `client.toString()`으로 페이지에 넣어 브라우저에서 그 보조 함수가 없기 때문. `wrangler.jsonc`에 `"keep_names": false`(설명 주석 포함)와 `"workers_dev": true`를 추가해 커밋 b214a3a → push → 자동 재배포 후 정상.
- 하위도메인 변경(요청 026): `rkdtjdwns1312.workers.dev` → `kokkiri-badminton.workers.dev`. 새 공개 주소 **https://kokkiri.kokkiri-badminton.workers.dev**. 옛 workers.dev 주소는 즉시 중단됨(공유한 적 없음).
- 비밀값: Settings → Runtime variables and secrets → "Add variable" 대화상자까지 열었으나, 비밀값 입력창에 글자를 넣는 동작은 Claude Code 보안 분류기가 차단해 사용자가 직접 입력하도록 안내.
- 변경 파일: `wrangler.jsonc`(keep_names, workers_dev), `README.md`(2단계 완료 표시·실제 주소), `CLAUDE.md`·`AGENTS.md`(실제 주소), `메인.md`, 이 기록.

## 4. 검증 결과 (Fable 직접 검토)

- 새 주소 GET 확인: `/` 200(홈 카드 3개 가운데 정렬, 마스코트, "함께한지 128일", 운영진권한 버튼, 콘솔 오류 없음), `/api/rankings` 61명(주밤 A 115, 로토 A 104), `/api/posts?kind=notice` 2건(옛 사이트 공지 그대로, 회원 화면에서 날짜 숨김 확인), `/api/posts?kind=schedule` 1건(테스트용 대진), `/mascot-play.png` 200.
- `/api/operator-login` → 503(비밀값 미설정 상태의 정상 동작). 비밀값 입력 후 200으로 바뀌어야 함.
- 로컬 `node check-login.mjs` PASS(커밋 전). GitHub Actions는 확인하지 않음.

## 5. 최종 점검 (Fable)

- 요청 025(양쪽에서 배포 가능한 구조)·026(주소에 개인 아이디 없음)·027(문서 저장소 안)·028(대행) 모두 충족. 남은 사용자 작업은 비밀값 2개 입력뿐.

## 6. 미완료 사항과 다음 단계

- 사용자: Cloudflare 대화상자에서 `EDITOR_KEY`(옛 운영진 링크의 `operate-` 뒤 문자열을 그대로 쓰면 기존 운영진 링크 형식 유지)와 `OPERATOR_PASSWORD`(운영진권한 버튼 비밀번호)를 Secret으로 입력하고 "Add variable and deploy". 그 뒤 회원 홈 → 운영진권한 → 비밀번호 → 운영진 화면 전환 확인.
- 회원 안내: 새 주소를 회원들에게 공지. 옛 Codex Sites 주소는 당분간 유지(옛 버전).
- 선택: `npm install`로 `package-lock.json`을 만들어 커밋하면 wrangler 버전이 고정되고 빌드 캐시가 동작(이 PC에 npm 없음 → Codex 또는 사용자 PC에서).
- Cloudflare 'Account details'의 하위도메인 표시는 캐시로 옛 값이 잠시 보일 수 있음.

## 7. Git 동기화·배포 결과

- 커밋 b214a3a push 완료, Cloudflare 자동 배포 성공. 문서 갱신 커밋은 이 기록 작성 직후 push.

## 핵심 명령

계정 설정을 대신 진행해 Cloudflare 첫 배포를 완료하고, 새 주소에서 개인 아이디를 제거한다.
