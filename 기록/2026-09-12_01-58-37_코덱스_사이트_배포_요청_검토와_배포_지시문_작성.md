# 코덱스 사이트 배포 요청 검토와 배포 지시문 작성

- 작업 시각: 2026-09-12 01:57 ~ 02:00 KST
- 작업 도구: Claude Code (Fable 직접 처리 — 조사와 문서 작성만, 코드 변경 없음)
- 관련 요청: 024

## 1. 요청 번호와 요청 요약

- 요청 024: "지금 테스트 화면처럼 만들어지게 코덱스 사이트에다가 적용해줘" — 로컬 미리보기에서 확인한 새 버전을 공개 홈페이지(코덱스 Sites)에 배포해 달라는 요청.

## 2. 계획 (Fable)

- 먼저 Claude Code에서 직접 배포할 수 있는지 읽기 전용으로 조사한다: 코덱스 sites-hosting 스킬 문서, git 자격 증명 저장소, 원격 설정.
- 가능하면 사용자 승인 아래 배포하고, 불가능하면 이유를 정확히 적고 코덱스에 붙여넣을 지시문을 만들어 준다.

## 3. 수행 내용과 변경 파일 (Fable 직접 처리)

- 조사 결과:
  - 코덱스 배포 절차(`sites-hosting/SKILL.md`, `references/publishing.md`): `create_source_repository_write_credential`로 임시 쓰기 자격을 받아 push → `package-site.mjs`로 압축 → `save_site_version` → 공개 사이트는 사용자 승인 후 `deploy_site_version` → `get_deployment_status` 확인. 이 도구들은 코덱스 Sites 커넥터의 네이티브 도구이며 "Site-owning conversation"에서만 호출하도록 되어 있다.
  - 이 PC의 git 자격 저장소에는 `git.chatgpt-team.site`용 자격이 없다(GitHub용만 존재). 원격은 `https://git.chatgpt-team.site/.../appgprj_6aa36ed5fee481918ea429fddff9a26b.git`.
  - 결론: Claude Code에는 위 커넥터 도구도, 자격 증명도 없어 배포를 실행할 수 없다. 사용자 인증 토큰을 꺼내 쓰는 우회는 하지 않는다.
- 작성 파일: `콕끼리 홈페이지 관리\코덱스_배포_지시문.md` — 코덱스 스레드에 그대로 붙여넣을 배포 요청 문구(상황, 포함 내용, 순서, 배포 후 확인 6항목)와 사용자 직접 확인 항목.
- `메인.md`: 요청 024 행 추가, '현재 상태'에 지시문 파일 연결.

## 4. 검증 결과 (Fable 직접 검토)

- 지시문의 커밋 수(6), 테스트 시각(01:53), 공개 주소, 포함 내용이 `메인.md` 현재 상태와 일치함을 확인. 비밀값 없음.

## 5. 최종 점검 (Fable)

- 배포는 미완이며, 사용자 행동(코덱스에 지시문 전송) 없이는 진행되지 않는다는 점을 명확히 보고한다.

## 6. 미완료 사항과 다음 단계

- 사용자가 코덱스 배드민턴 스레드에 `코덱스_배포_지시문.md`의 본문을 붙여넣는다.
- 코덱스가 배포를 마치면 `메인.md` 현재 상태를 배포 완료로 갱신하고 기록을 남긴다(지시문에 포함).

## 7. Git 동기화·배포 결과

- 코드 변경 없음, 새 커밋 없음. 배포 미완(코덱스 필요).

## 핵심 명령

테스트 화면과 같은 새 버전을 코덱스 사이트에 배포한다 — Claude Code에서는 불가, 코덱스용 배포 지시문 제공.
