# 요청 083 — MVP 문구를 대진표 목록으로 이동

- 시작: 2026-09-14 01:04 KST
- 요청: 대진 상세 제목 아래 MVP 줄을 없애고, 이전 대진표 목록의 각 제목 아래 같은 문구를 표시한다. '이번 정모'는 생략한다.

## 계획과 모델 분담

- GPT-5.6 Luna(low) 1명: 클라이언트 표시 위치·문구 수정.
- GPT-6 Astra 본체: 목록 API에 기존 MVP 배열 연결, 테스트·문서·최종 검토·배포.
- 홈 MVP 배너는 기존 별도 요구(077)를 유지한다. 점수·MVP 선정·정산·운영 데이터는 변경하지 않는다.

## 수행 내용과 변경 파일

- `dist/server/boards-client.js`: 대진 목록 제목과 날짜 사이에 기존 작은 메달·반짝임 MVP 줄 표시. 상세의 MVP 줄 삭제. 마감 여부·배열 유무를 확인하고 이름은 HTML 이스케이프한다.
- `dist/server/boards.js`: 목록 조회 SQL에 기존 payload의 MVP를 포함하고 응답에서는 배열로 반환(없으면 빈 배열). 추가 상세 API 호출·마이그레이션 없음.
- `check-login.mjs`: 마감 후 목록 MVP가 상세와 일치하는지, 공지는 빈 배열인지, 마감 취소 후 목록에서 사라지는지 회귀 검사 추가.
- `메인.md`, `프로젝트_인수인계.md`, 이 기록: 요청·API·표시 규칙 갱신.

## 검증 및 최종 점검

- Luna 클라이언트 문법검사 PASS. Astra diff 검토에서 목록 문구에 '이번 정모'가 남은 것을 발견해 `MVP`로 보정했다. 홈의 기존 문구는 별개로 유지했다.
- 수정 후 번들 Node `check-login.mjs --serve` PASS(필수 로그인·정산·순위/20점 보호 검사와 새 MVP 목록 검사 포함).
- 로컬 브라우저: 마감된 5개 목록에 MVP 표시, 마감 취소된 2개는 미표시. 9/12 명단 6명 확인. MVP 줄이 각 제목 바로 아래이며 글자 크기 14.4px임을 DOM 위치로 확인.
- 목록의 9/12 글 클릭 → 상세 MVP 줄 0개, 제목·점수 반영 완료·대진 유지 확인.
- 회원 공지 목록 2개에 MVP 0개, 날짜 표시 0개(기존 규칙 유지). 브라우저 오류 로그 0개.
- 실제 공개 경기 마감·데이터 수정은 하지 않았다.

## Git 동기화·배포 결과

- 시작 HEAD: 3eca903. 번들 git pull --ff-only origin main: Already up to date.
- 기능 커밋 `edd67b21ecac9e2661e7313da70700937b8b8317`: `Move MVP label from schedule detail to list`. push origin main 성공, ls-remote origin refs/heads/main과 HEAD 일치 확인.
- 2026-09-14 01:10 KST 공개 반영 확인: 목록 API가 9/12 정모의 MVP 6명 배열 반환. 공개 대진표 목록 제목 바로 아래 `🥇 MVP 로토 시오 야키 슝슝이 동이 소고기` 표시(14.4px). 해당 글을 실제 클릭해 상세 MVP 줄 0개·원제목 유지 확인. 브라우저 오류 로그 0개.
- 완료 기록은 별도 문서 커밋으로 main에 함께 보존한다. 로컬 검증 서버 종료.

## 미완료 사항과 다음 단계

- 이 요청의 미완료 작업 없음. 공개 화면이 이전 상태라면 새로고침하면 된다.

## 핵심 명령

대진 상세의 MVP 문구를 대진표 목록 제목 아래로 옮기고 '이번 정모'를 뺀다.
