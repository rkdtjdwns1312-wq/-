# 117 — 회원별 DB 기반 시드 점수 변화 그래프

## 요청과 시작 상태

- 시드현황에서 이름 클릭 시 점수 변화 꺾은선그래프. 첫 요청은 최근 1년이었으나 사용자가 최근 20주로 정정하고 구현 요청 확정. 최근 20주(140일)가 기본으로 한눈에 보이고 이전 기록은 가로 스크롤로 조회.
- 시작 f1f40b9 clean, git pull --ff-only origin main 최신. 기존 회원권한(116) 유지, 운영 경기/점수 시험 쓰기는 하지 않는다.

## 설계와 분담

- 현재 정산 이벤트/수동 변경 DB에는 전후 점수가 있으나 마감 취소 시 정산 이벤트는 삭제된다. 원본을 건드리지 않는 별도 점수 이력 테이블을 두고 기존 보관 기록을 이관, 앞으로 DB 점수 변경 트리거로 확정된 실제 값의 변화를 누적한다.
- 기록 없는 과거를 임의 생성하지 않는다. 현재 기준점은 DB 스냅샷으로 명시하고 게스트→회원 이관도 ID 연결로 보존한다. 점수·승패·순위 계산 규칙은 변경하지 않는다.
- 회원 보호된 조회 API, 20주 단위 지연 로드, 기본 최근 20주·왼쪽으로 과거, 모바일 터치/키보드/이전 기록 버튼 보조. 그래프는 외부 서비스 없이 자체 SVG.
- 본체 서버·DB·통합 검사·배포/문서, Terra UI와 UI 검사, Astra 설계/최종 검토.

## 현재 상태

- DB 원장/기존 기록 이관/회원 보호 API/백업 포함/20주 그래프 통합 구현. 저장된 공개 과거 기록의 정확한 범위는 배포 뒤 읽기 검증한다.
- 최초 Astra 설계 검토 실행은 모델 용량 부족으로 실패(미수행). 재시도한 Astra 검토에서 UI 시험 교착, 팝업 너비, 마지막 과거 구간 조회 후 이전 버튼 문제를 발견해 모두 수정했다. Terra UI 구현과 본체 통합 보완 후 최종 Astra PASS, 남은 수정 필요 지적 없음.

## 검증 진행

- 전체 `check-login.mjs` PASS(21:53 KST): 기존 정산/취소/게스트 이관/대진/실시간/긴급교체/회원 인증 회귀 및 새 이력 검사 포함. 이후 화면 가독성 개선 후 최종 재실행한다.
- 새 서버 검사: 이관 마이그레이션이 원본 점수·순위·명단·게시글을 수정하지 않음, 재실행 중복 없음, 기록 없는 날짜 미생성, 기존 20점 미만 과거 보존, 140일 반열린 구간/희소 다년간 페이지/경계 중복 방지, 비회원 401/활성 명단 404, 수정 사유 비공개, GET 무변경, 트랜잭션 롤백, 실제 정산→게스트 이관→마감 취소 이후 상승·원복 기록 유지.
- 150주 합성 데이터는 로컬 메모리 DB만 사용. PC 회원/게스트 이름 팝업, 최근 20주/이전 구간 추가 조회 확인. 모바일 눈금 가독성과 이전 버튼의 이동 동작 추가 보완.
- 공개 데이터 배포 전 읽기 기준 SHA-256: rankings `27e73fd4ff19a3d92679c851cd78cd2be54ffe70b9ca0854a448f57546402ca2`, people `83361a045b20098bf1ac37ef4361a31e7eb7a373ea5f260c59fff8f3ff5ea90a`, schedule `3bf962c7675f180e1c85f8654119ee775eab45826b3f0ca344091a233535aa6d`, notice `d7010323d9b2f3501b105e9b831d4b5843dfc3ed8d50a553240558de7fab5c7b`, live(serverNow 제외) `61060550e62c866a762a886c04992b1f7dcb9cc208944cbd3582a09fa961dbeb`. 기준 수집 시 운영 데이터 쓰기 없음.

## 핵심 명령

- 기능 커밋 c2b11ee7ad79161bf41669804e5ece00ba779876 push. GitHub test 22:11:30/Cloudflare Workers Builds 22:11:41 KST success. 공개 DB 이력 140일 범위 및 현재 점수 일치, 회원/게스트 200·비회원 401, 배포 전후 데이터 5종 해시 모두 동일.
- 공개 브라우저의 회원 입장→이름 클릭→그래프 확인. 주밤은 9/13 기준 115·정산 +1·116, 호잇/몽구는 기존 이력이 없어 9/19 스냅샷부터 표시된다. 없는 과거를 만들어내지 않음.
- 공개 검증 중 텍스트 기록을 펼치면 ResizeObserver 재배치가 접힘 상태로 재생성하는 문제 발견. 기존 details.open을 유지하도록 수정하고 회귀 검사 추가. 전체 검사·Astra 추가 검토 PASS, 실제 로컬 펼침→320px 변경에도 열림 유지·접기·가로 넘침 없음 확인. 후속 코드/문서 push 후 공개 재확인 예정.
- 22:09 KST 구현·검증 완료, 배포 전. 실제 PC/320px에서 150주 합성 기록 8구간 로드·최신/이전 왕복·키보드 점 선택·재열기·가로 위치 유지·화면 크기 전환 검증 PASS. 가로 넘침/JS 오류 없음.
- 반복 검사에서 SQLite/호스트 시계의 수 ms 차이로 방금 기록된 변경 누락을 발견. 단일 SELECT와 DB 시각으로 창·명단·이력을 함께 읽도록 수정. 호스트 시계 5초 지연 회귀 추가, 서버 검사 20회 연속 및 전체 검사 PASS.
- 번들 git pull --ff-only origin main: 최신.
- node check-score-history.mjs: PASS.
- node check-login.mjs: 최종 PASS.
- Astra/브라우저 최종 검증: PASS.
- 예정: 문서 포함 commit·push → 공개 읽기 검증.
