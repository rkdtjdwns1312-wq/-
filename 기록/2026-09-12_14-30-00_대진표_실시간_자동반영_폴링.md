# 대진표 승패 실시간 자동 반영(5초 폴링)

- 작업 시각: 2026-09-12 14:20 ~ 14:32 KST
- 작업 도구: Claude Code (Fable 본체 직접 처리 — 클라이언트 수정, 테스트·로컬 브라우저 실동작 확인)
- 관련 요청: 050 (질문 048의 후속 구현)

## 1. 요청 번호와 요청 요약

- 요청 050: 여러 명이 승 버튼을 누를 때 새로고침 없이 서로의 기록이 보이도록 자동 새로고침(권장안: 5초 주기).

## 2. 계획 (Fable/본체)

- 대진표 상세(미정산 schedule)를 보는 동안 5초마다 `/api/posts/:id`를 확인해 version이 바뀌었으면(누군가 결과 기록/정산) 화면을 갱신한다.
- 갱신은 version이 실제로 바뀌었을 때만 `detail()` 재렌더(viewRound 유지 → 보던 라운드 그대로). 진행 중인 내 기록·다이얼로그·저장 중에는 건너뛴다.
- 폴링 정지: route 진입, 편집 모드 진입(editSchedule/editNotice), 정산 완료 시.

## 3. 수행 내용과 변경 파일 (Fable 직접 처리)

- `dist/server/boards-client.js`:
  - 클로저 `pollTimer`, `recordingResult`, `stopPoll()` 추가.
  - route()·editSchedule()·editNotice() 진입 시 `stopPoll()`.
  - detail() 끝에서 schedule && !settledAt이면 5초 `setInterval` 시작: `recordingResult||saving||dialog.open`이면 skip, myToken!==routeToken이면 정지, version 또는 settledAt 변화 시 `stopPoll();detail(d.id,routeToken)`로 갱신.
  - recordResult: `recordingResult` 플래그(폴링 가드), saveResult 응답의 version으로 `d.version` 갱신(내 변경으로는 폴링 재렌더 안 되게), finally에서 플래그 해제.

## 4. 검증 결과 (본체 검토 + 테스트 + 브라우저 실동작)

- `node check-login.mjs` PASS(클라이언트 파싱, 스코프 정상).
- 로컬 브라우저(회원 화면): 2라운드로 전환 후, 페이지 내 fetch로 "다른 기기"가 1라운드 결과(0-0=a) 기록(version 4) → 6.5초 뒤 자동으로 1라운드 패널에 `has-result result-a`·승 버튼 picked 반영, 그동안 보던 2라운드(activeTab 2R, 2라운드만 표시)는 그대로 유지 확인.

## 5. 최종 점검 (Fable/본체)

- 새로고침 없이 남의 기록이 5초 내 반영, 보던 라운드·진행 중 동작은 방해하지 않음. 정산되면 폴링 종료. 편집 모드에서는 폴링이 화면을 덮지 않음.
- 부하: 상세를 보는 동안에만 5초 간격 GET 1회. 변화 없으면 재렌더 없음.

## 6. 미완료 사항과 다음 단계

- 없음. (주기 변경 요청 시 setInterval 값만 조정.)

## 7. Git 동기화·배포 결과

- 커밋 후 GitHub push → Cloudflare 자동 배포.
