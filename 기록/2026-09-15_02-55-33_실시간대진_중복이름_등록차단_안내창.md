# 실시간대진 중복 이름 등록 차단 안내창

- 요청: 097
- 시작: 2026-09-15 02:55 KST, 요청 096 진행 중 추가 요청.

## 요청과 구현 기준

- 현재 코트에 등록된 이름은 다른 코트나 다음 대진 대기열에 다시 등록할 수 없다. 네 명이 모이기 전 빈자리가 남은 코트도 포함한다.
- 앞뒤 공백을 제거한 이름으로 서버가 검사한다. 거절된 입력은 DB와 버전을 바꾸지 않는다.
- 안내창 문구는 `현재 게임중인 회원입니다. 등록할 수 없습니다.`이며 `확인` 버튼으로 닫는다. 이름/코트 입력은 유지하여 바로 고칠 수 있다.
- 대기열 내 기존 중복 차단도 유지한다. 코트 종료로 이름이 현재 코트에서 빠진 뒤에는 다시 등록할 수 있다.
- 기존 정모 승패·순위·출석·시드는 변경하지 않는다.

## 진행

- 본체: 서버 검사·회귀 검사·문서·통합 검증.
- Terra: 기존 096 담당 범위 안에서 안내창과 실제 UI 함수 검사 추가.
- 공개 명단을 시험용으로 등록하지 않고 로컬 가상 DB에서 검증한다.

## 구현·검증 결과 (배포 전)

- 서버가 전체 코트 이름을 검사하여 요청 문구로 거절한다. 대기열 중복은 별도 문구로 유지한다.
- Terra가 단일 확인 버튼 안내창과 입력 보존을 구현했다. Astra 검토 후 본체가 원격 종료 시 중첩 알림 정리, 페이지 이동 후 늦은 오류 무시를 보완했고 재검토 PASS.
- API 검사: 앞뒤 공백·빈자리 있는 코트·게임 중 코트·대기열 우회·동시 같은 이름 입력·거절 시 DB/버전 불변·게임 종료 뒤 재참가 PASS.
- 실제 UI 함수 검사: 정확한 문구·단일 확인·입력과 선택 보존·원격 종료 시 두 창 정리·페이지 이동 후 알림 방지 PASS. 전체 `node check-login.mjs` PASS.
- 로컬 PC/390px 모바일에서 가상 이름 중복 입력 후 지정 문구와 확인 버튼을 시각 확인했다. PC에서 확인을 누르면 기존 이름/코트 선택이 남는 것을 확인했다.
- 실제 운영 명단·점수는 시험으로 변경하지 않았다. 공개 배포 확인은 다음 단계다.

## 공개 반영 확인 (2026-09-15 03:06 KST)

- `4ddc566` GitHub main push·Cloudflare 배포 확인.
- 공개 배포된 코드의 `현재 게임중인 회원입니다. 등록할 수 없습니다.`와 `liveDuplicateOk` 확인 버튼을 읽기 검사했다. 공개 세션은 닫혀 있으므로 중복 등록 시험을 위해 실제 이름을 쓰거나 개방하지 않았다.
- 실제 클릭/거절/입력 보존은 로컬 PC와 390px 화면, 동시 입력/재참가는 가상 DB 테스트에서 검증했다. 최신 코드의 모바일 중복 안내 중 원격 종료로 모든 입력창 정리도 확인했다.
- 구현·검증·배포 완료. 상세 코드·MD는 저장소에 함께 보관한다.

## 핵심 명령

현재 코트 중복 이름 서버 차단 → 지정 문구/확인 안내창 → 필수 검사 PASS → 096과 함께 문서 포함 commit·push → 공개 확인.
