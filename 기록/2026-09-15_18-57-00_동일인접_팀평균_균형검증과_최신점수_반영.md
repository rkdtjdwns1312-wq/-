# 동일·인접 팀 평균 균형 검증과 최신 점수 반영

- 요청 100, 2026-09-15 18:57 KST 착수.
- 동일/인접 자동 편성에서 강한 두 사람이 한 팀으로 묶이는 문제를 검증하고 잘못된 알고리즘을 보완해 달라는 요청.
- 작업 전 clean, `git pull --ff-only origin main`: 89b5960 → 4dc54a7. 다른 작업의 099 자유대진 변경 및 일요일 Cron 보완을 보존한다.
- 최신 필수 `node check-login.mjs` 전체 PASS.

## 확인 기준

- 공개 DB 조회: 주밤 116, 아식스 110, 시오 104, 두진 97점.
- 주밤+아식스 vs 두진+시오: 평균 113 vs 100.5, 차이 12.5점.
- 주밤+두진 vs 아식스+시오: 평균 106.5 vs 107, 차이 0.5점으로 가장 균형적이다.
- 주밤+시오 vs 아식스+두진: 평균 110 vs 103.5, 차이 6.5점.
- 회원 이름/점수/대진/승패 등 실제 운영 데이터는 시험으로 수정하지 않는다. 기존 저장된 대진을 임의 재편성하지 않는다.
- 기존 같은 4명/인접 그룹 규칙, 평균 차이 우선·동률 때 파트너 반복 최소화, 늦참·휴식·수기 수정 허용은 유지한다.

## 진행

- 순수 생성기와 실제 UI의 입력/캐시/렌더링 경로를 분리해 검증한다.
- 이전 요청 098의 GitHub 코드 반영과 새 공개 클라이언트 문구를 확인했다. 완료 기록도 함께 마무리한다.

## 재현과 원인

- 현재 DB 숫자 점수를 순수 생성기에 직접 주면 동일·인접은 예시 24개 입력 순서에서 모두 주밤+두진 vs 아식스+시오를 선택했다. 각 4인조의 세 가지 분할을 완전탐색해 비교해도 기존 강약 혼합 후보가 최소 차이를 포함하므로 그룹 규칙을 전면 변경하지 않았다.
- `getPeople`가 처음 읽은 명단/점수를 페이지가 열린 동안 계속 캐시했다. 실제 클라이언트 함수에 오래된 점수→DB 변경을 주입하면 오래된 점수로 편성할 수 있었다. 사용자의 당시 화면 메모리 값은 남아 있지 않아 그 사례의 유일한 원인이라고 단정하지 않는다.
- 더 직접적인 우회 경로: `＋ 코트 추가`는 휴식자/참여자에서 네 명을 고른 뒤 `r.g.push(four)`로 저장하여 팀 평균 계산을 전혀 거치지 않았다. 강한 두 사람이 앞에 있으면 같은 팀이 된다.
- 공개 `뉴키 대진 테스트`는 설정 3코트, 실제 매 라운드 4대진이며 2R/3R/4R의 네 번째 대진에서 평균 차이 28.5/25.5/10.5, 가능한 최소 차이 16.5/5.5/2.5를 확인했다. 저장된 상태만으로 사용자의 수기 편집 여부까지 단정하지는 않지만 추가 코트 코드에서 같은 불균형을 재현했다. 기존 공개 대진은 수정하지 않았다.

## 보완

- 선택창을 열 때와 실제 대진 생성 직전에 DB 점수를 새로 읽는다. 후발 응답이 최신 캐시를 덮지 못하게 읽기 순번을 둔다. 시드현황 게스트 목록도 새로 읽는다.
- 생성 중 중복 클릭, 취소/화면 이동 뒤 응답, 명단 삭제/이름 변경, 읽기 실패, 선택 인원/코트 변경을 검사하고 오래된 점수로 임의 생성하지 않는다.
- 숫자 문자열 점수도 숫자로 정규화한다. 현재 공개 DB 응답은 숫자 타입이므로 이 항목은 방어적 보완이다.
- 공통 `balanceFour`를 기본 생성과 코트 추가가 함께 사용한다. 추가할 네 명은 기존 규칙대로 고르고, 동일·인접에서는 그 안의 팀 평균 차이가 최소인 조합을 선택한다. 차이가 같을 때만 이전 파트너 반복을 줄인다.
- 수기 드롭다운으로 이미 정한 다른 경기의 팀은 자동 재배열하지 않는다. 랜덤, 휴식 우선순위, 늦참, 시드/정산 규칙도 그대로다.
- DB 미등록 즉석 참가자는 기존의 미정 점수 처리 대상이다. 실제 점수 기반 균형을 원하면 시드현황에 먼저 점수를 등록해야 한다. 수기 편집으로 불균형을 만드는 것을 금지하지 않는다.

## 검증·분담

- Terra: 순수 생성기/공통 helper/세 조합 최적성 회귀. Codex 본체: 실제 입력 캐시와 추가 코트 UI·비동기 안전성·통합·문서. Astra: 최신 diff 읽기 전용 최종 검토.
- 실제 UI 함수에서 아식스의 이전 20점 캐시→최신 110점 조회를 모사하여 평균 106.5 vs 107 조합을 확인했다. 20점은 재현용 가상 입력이며 실제 과거 점수를 뜻하지 않는다.
- 로컬 브라우저 1코트 2라운드(동일·인접)에서 예시 네 명이 두 라운드 모두 주밤+두진 vs 아식스+시오로 표시됨을 확인했다. 공개 DB에 생성/승패/정산 시험을 하지 않았다.
- 최종 필수 검사 및 실제 추가 코트 UI 회귀 PASS. Astra의 추가 비정렬 4인 조합 192건·점수/수기 팀/승패/늦참 보존·조회 실패/중복/저장·이동 방어 검토 PASS.
- 19:11 KST 로컬 브라우저에서 원래 수기 팀(주밤+아식스 vs 두진+시오)을 그대로 둔 채 `＋ 코트 추가`로 주밤+두진 vs 아식스+시오가 생성됨을 확인했다. 로컬 저장 API도 두 팀 배치를 그대로 보존했다.
- 19:12 KST 기능 커밋 `be7ef8a` GitHub main push. 19:13~19:14 KST 공개 HTML에 새 `balanceFour` 포함, 공개 홈과 주요 API 5개 HTTP 200 확인.
- 배포 전후 회원/게스트 해시, 공지 version 7·4, 대진 version 2·2·16·2, 자유대진 version 32·열림 상태 모두 동일하다. 운영진의 현재 진행 데이터와 기존 저장된 대진을 변경하지 않았다.
- 작업용 4195 서버·메모리 시험 데이터·로컬 탭을 정리했다. Terra 구현·Astra 최종 검토·필수 검사·브라우저 검증을 완료하고 공개 반영 완료로 기록한다.

## 공개 기준 상태

- 19:04 KST 회원 64명·게스트 32명, 이름/점수/시드/현재 순위 SHA-256 `9763570d1f903465656cb70cb3b1e530161d72a21ccd5945575d38f0a68aeb30`(098 전과 동일).
- 공지 version 7·4, 대진 4건 version 2·2·16·2. 자유대진은 사용 중(isOpen true, version 32)이므로 조회만 하고 이름/코트/대기열을 시험으로 바꾸지 않는다. 098 아침의 닫힘 상태와 달라졌다는 이유로 복원하지 않는다.

## 핵심 명령

현재 DB 점수 비교 → 동일·인접 재현 → 원인에 맞는 최소 보완 → 회귀·필수·브라우저 검사 → MD 포함 commit·push → 공개 확인.
